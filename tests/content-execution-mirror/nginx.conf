server {
    listen 80;
    root /usr/share/nginx/html;
    location /summary/ {
        proxy_pass http://summary-versioning-execution-api-1:8080/;
    }
    location / {
        try_files $uri $uri/ /index.html;
    }
}
