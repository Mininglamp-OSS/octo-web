# DAP octo-web-native event table

Single-source convergence artifact for the DAP UI telemetry integration
(PR #1407 established §1–§5; extended by dap350 / PR #1443, which added the fleet + doc
central-mapping rules now enumerated in §6).
Review **this table**, not the diff: it enumerates every wired octo-web-native event,
the channel it fires on, the exact success condition, the negative/failure edge it must
NOT fire on, its reset/dedup rule, and the test that pins it.

**Channels** (each event lives in exactly one — cross-channel duplication is a bug the
`channelUniqueness` test guards against; no registered exceptions remain — `channel_opened`
was the last one and is now unified to a single **imperative** channel with two
runtime-disjoint emit sites, see that row):
- `fetch` — first-party same-origin 2xx on a path → event (FetchRules.ts). Path selects the name, never stored.
- `body` — request-body top-level key/value → event (BodyRules.ts). `equals` discriminators whitelist the value; `hasKeys` is presence-only.
- `track(testid)` — global capture-phase click/keyboard delegation on `data-testid` (TrackRules.ts).
- `data-track` — DOM attribute on the element; delegation covers mouse + keyboard; `data-track-ignore` subtrees suppress.
- `imperative` — explicit `Dap.shared.track(event, props)` at the confirmed-action site.
- `helper` — gated helper (trackMessage.ts sendack-consumed intents; summaryApi.ts envelope-code gate).

**Privacy**: props are `{}` except an opaque `object_id` join key on a few events
(channel/group id, account uid) and the bounded, non-content diagnostics explicitly
registered in this table. `smart_summary_quality_gate` may carry `task_id`,
`finish_status`, `gap_count`, optional `first_gap_kind`, and existing entry-context
dimensions. `first_gap_kind` is normalized to the documented category allowlist
(`channel`, `coverage`, `truncation`, `output_truncation`, `dropped`, `citation`,
`tool_error`, `evidence`, or `other`). It never carries `gaps[].detail`, summary
content, keywords, or PII.
See issue #1406 (privacy constraint amended round-6 to permit opaque `object_id`);
the quality-gate row below records the additional bounded diagnostic envelope.

**`channel_id` 归一约定 (#1452 R10 P2-2)**: 本 PR 新增/收口的所有命令式事件统一发 **bare
channel_id** —— 即经 `stripSpacePrefix()` 去掉 Space 部署下的 `s<32hex>_` 前缀,取裸 group_no /
uid。覆盖 `conversation_muted` / `conversation_pinned` / `channel_search_query` /
`group_bot_free_mention_toggled` / `subchannel_created` / `subchannel_opened`(两入口)。
目的是让这些事件在 Space 部署下能按同一 `channel_id` 跨事件 join(否则同一频道在带前缀 / 不带
前缀两种形态间对不上)。注意这**不是**与后端某接口对齐:`_search_` / group setting 等接口各自的
strip 口径并不一致(`SearchService` 发 raw、`ChannelSettingService` 仅对 Person strip),故不能
以「跟接口一致」为由——统一到 bare 是为数仓 join。**唯一例外**:既有已上线的
`channel_opened`(`object_id`)保留 **raw** channelID,不改其口径以免二次改变已发布指标的
序列(见该行);跨事件 join 到 `channel_opened` 需在查询侧自行 strip。

**Infra events** (not user intents): `app_launched` (once, post-login — deferred so
anonymous login-page visitors get no device_id / tokenless POST), `http_request`,
`page_view`, `page_leave`.

**Test coverage note**: events marked "—" have no *dedicated* positive-emission test.
Many are still covered indirectly — the `fetch` events by the generic FETCH_RULES
"every rule is hit by its own concretized path" invariant, and several imperative UI-only
events by the negative `channelUniqueness` / FetchRules guards asserting they must NOT
appear in the path channel. Genuinely unpinned positive fires are the honest gap this
table surfaces for follow-up.

Total rows in this table: **264** (`grep -c '^| \`'` — includes the 4 infra rows; removed-rule
tombstones kept as ~~struck~~ history sit outside this grep and are not counted). §1–§5 cover the im/base + summary +
market events; §6 covers the fleet (Loop) + doc central-mapping events wired by dap350
(PR #1443), which the T1 re-review confirmed are captured by the shared octo-web bundle and
are therefore no longer out of scope. The imperative / data-track fleet/doc events emitted
from the dmloop / docs module source (not the central rule tables) are owned by B-loop /
B-docs and are not restated here.


## 1. `fetch` + `body` channels (path / request-body → event)

| Event | Channel | Fires exactly when | Must NOT fire (negative/failure edge) | Reset/dedup | Pinning test |
|---|---|---|---|---|---|
| `apps_module_entered` | imperative | Nav callback fires when the user switches to the Apps menu (`menus.id === "appbot"` && `!isReentry`) — apps/web Main/index.tsx + tab_low_screen.tsx (desktop NavRail + low-screen tab, symmetric) | R12 🔴 P1-3 moved off `fetch`: GET `/app_bot/available` is re-pulled by `useAppBots` on every space-change (mittBus "space-changed") and the Apps page stays mounted (MainContentLeft toggles display only), so after one visit any space-switch mis-fired it; conversely re-entering Apps from a mounted state pulled no GET → undercount. Repeat-click of the active menu (`isReentry`) | `!isReentry` gate (mirrors contacts_module_entered) | FetchRules.test.ts — it('GET /app_bot/available 不产出 apps_module_entered...') pins endpoint → undefined; channelUniqueness.test.ts self-check asserts imperative scan covers it |
| `contact_add_friend_clicked` | fetch | POST `/api/v1/friend/apply` returns 2xx (friend request sent) | Non-2xx/cross-origin; not on GET `/users/:id` profile pulls (contact_opened removed to avoid double-count) | None — per successful request | — (generic FETCH_RULES invariant only) |
| `contact_add_friend_succeeded` | fetch | POST `/api/v1/friend/sure` returns 2xx — the acceptor confirms an incoming request in「新的朋友」(`NewFriend/vm.tsx friendSure` → datasource `friendSure` → `apiClient.post("friend/sure")`). Completion terminal state, pairs with `contact_add_friend_clicked` (start) for the DAP 达成率 | Non-2xx/cross-origin. Distinct from bot-side BotFather `/approve`·`/reject` handling (那是 `bot_friend_request_handled`, imperative, botfather channel only). Single call site (NewFriend confirm) | None — per successful accept | — (generic FETCH_RULES invariant only) |
| `conversation_cleared` | imperative | `Dap.shared.track('conversation_cleared', {})` after a real clear gesture succeeds — clearChannelSettingMessages (after clearConversationMessages) + Pages/Chat/vm.clearMessages (conversation-list "关闭并清空" 右键项) | R12 🔴 P1-1 moved off `fetch`: POST `/api/v1/message/offset` is the low-level clear endpoint but the same clearConversationMessages is also invoked by **remove-friend** (module.tsx removeFriend onOk) as a side effect, so the path rule mis-counted friend-removal as a clear. Remove-friend calls the provider directly, bypassing both gesture entry points → stays silent | Fires once per real clear gesture; not on the no-conversation path | channelSettingActions.test.ts — it('clears conversation messages when a conversation exists') asserts emit once + it('does nothing when clearing ... without a conversation') asserts no emit; FetchRules.test.ts pins POST `/message/offset`→undefined; channelUniqueness self-check covers scan |
| `conversation_left` | imperative | `Dap.shared.track('conversation_left', {})` right after `exitChannel` (group exit) or `leaveThread` (subchannel leave) resolves — channelSettingActions.ts | R11 🔴 moved off `fetch`: old POST `/groups/:id/exit` + DELETE `/conversations/:id/:id` rules double-counted one exit (exit + best-effort deleteConversation both matched), mis-counted "close chat" (onCloseChat shares the DELETE, not a leave), and caught subchannel/DM leave only via the fallback DELETE. No DM "leave" gesture exists (close-chat is a hide) → covers group-exit + subchannel-leave only | Fires once per successful exit/leave; **independent of** the best-effort deleteConversation (emit precedes it) | channelSettingActions.test.ts — it('exits a group ... even if delete fails') + it('leaves a thread ...') assert emit once; it('updates thread name without emitting conversation_left'); FetchRules.test.ts — describe('conversation_left 已移出 path 通道') pins both endpoints → undefined |
| `conversation_muted` | imperative | `Dap.shared.track('conversation_muted', { action, channel_id })` after `muteChannel` resolves in `channelSettingActions.muteChannelSetting` — the shared funnel for all mute entry points (会话列表右键 + 设置面板 + 子区设置) | Failure of the underlying `muteChannel` (emit follows a resolved await); no other entry path exists | Fires once per successful mute gesture; `action` ∈ `mute`/`unmute` carries direction (both directions counted); `channel_id` = `stripSpacePrefix(params.channel.channelID)`(spec 关键属性;bare) | channelSettingActions.test.ts — it('emits imperative conversation_muted/pinned with the direction action') asserts both directions + channel_id; channelUniqueness self-check asserts imperative scan covers it + exclusivity vs BODY_RULES |
| `conversation_pinned` | imperative | `Dap.shared.track('conversation_pinned', { action, channel_id })` after `topChannel` resolves in `channelSettingActions.topChannelSetting` — shared funnel for 列表右键 + 设置面板置顶开关 | Failure of the underlying `topChannel`; no other entry path | Fires once per successful pin gesture; `action` ∈ `pin`/`unpin`; `channel_id` = `stripSpacePrefix(params.channel.channelID)`(spec 关键属性;bare) | channelSettingActions.test.ts — same test asserts both directions + channel_id; channelUniqueness self-check covers scan + exclusivity |
| `conversation_remark_edited` | body | Same endpoints with top-level key `remark` present (any value, incl. empty) on 2xx | `remark` key absent; non-whitelisted endpoint/non-JSON body | Presence-only (edit semantics, no off state — kept `hasKeys`); per 2xx | BodyRules.test.ts — it('PUT /groups/:id/setting 单键 ...') + it('关闭(值=0)...') asserts `remark:''`→edited |
| `conversation_saved_to_contacts` | body | Same endpoints with body `save==1` on 2xx (user saves conversation to contacts) | `save:0` (unsave) — `equals{values:[1]}` guards; non-whitelisted endpoint/non-JSON body | Value-whitelist [1] dedups off; per 2xx | BodyRules.test.ts — it('PUT /groups/:id/setting 单键 ...') + it('关闭(值=0)...') |
| `group_admin_added` | fetch | POST `/api/v1/groups/:id/managers` returns 2xx (promote a group admin) | Non-2xx/cross-origin | None — per successful request | — (generic FETCH_RULES invariant only) |
| `group_admin_removed` | fetch | DELETE `/api/v1/groups/:id/managers` returns 2xx (demote a group admin) | Non-2xx/cross-origin | None — per successful request | — (generic FETCH_RULES invariant only) |
| `group_announcement_edited` | body | PUT `/api/v1/groups/:id` with top-level key `notice` present on 2xx (edit group announcement) | body carrying `name` instead (→ `group_name_edited`); `notice` absent; non-JSON body | Presence-only; `name`/`notice` mutually exclusive (single-key partial update); per 2xx | BodyRules.test.ts — it('PUT /groups/:id name→改名, notice→改公告') |
| `group_avatar_edited` | imperative | `Dap.shared.track('group_avatar_edited', {})` in ChannelAvatar's two component-owned HTTP branches: saveUploadedAvatar else-branch (uploaded image) + saveCustomAvatar after updateChannelAvatarCustom (generated/clear) | R12 🔴 P1-5 moved off `fetch`: POST `/groups/:id/avatar` had two callers — real edit (ChannelAvatar) **and** group creation (groupCreateRuntime.uploadGroupAvatar); the path rule couldn't tell them apart, so picking an image at create-time mis-counted as an edit. It also undercounted: generated/clear edits go through PUT `/groups/:id` (avatar_text/avatar_color) which the group body rule (name/notice only, no fallback) never emitted. Create flow routes through onFileUpload/onDraftSave → early return, silent | Fires once per successful edit branch | FetchRules.test.ts — it('POST /groups/:id/avatar 不产出 group_avatar_edited...') pins endpoint → undefined; channelUniqueness self-check covers scan |
| `group_bot_admin_added` | fetch | PUT `/api/v1/groups/:id/bot_admin/:id` returns 2xx (grant bot admin) | Non-2xx/cross-origin | None — per successful request | — (generic FETCH_RULES invariant only) |
| `group_bot_admin_removed` | fetch | DELETE `/api/v1/groups/:id/bot_admin/:id` returns 2xx (revoke bot admin) | Non-2xx/cross-origin | None — per successful request | — (generic FETCH_RULES invariant only) |
| `group_bot_free_mention_toggled` | imperative | `Dap.shared.track('group_bot_free_mention_toggled', { channel_id, enabled })` in groupManagementActions.setGroupManagementAllowNoMention after setAllowNoMention resolves — the sole write funnel (GroupManagement panel switch) | Write rejects (no track); `enabled`/`channel_id` come from front-end context, not body | Moved off `body` (R? / review B): the body channel can only emit the event name, never the spec 关键属性 `enabled`/`channel_id` (privacy invariant — never emit body values), so on/off + which channel were unmeasurable. Imperative收口 carries both. Fires once per successful toggle; `channel_id` bare(stripSpacePrefix) | groupManagementActions.test.ts — it('sets allow-no-mention and then refreshes channel info') asserts {channel_id, enabled}; BodyRules.test.ts pins `allow_no_mention:1`→undefined; channelUniqueness self-check |
| `group_dissolved` | fetch | DELETE `/api/v1/groups/:id/disband` returns 2xx (dissolve group) | Non-2xx/cross-origin | None — per successful request | — (generic FETCH_RULES invariant only) |
| `group_md_edited` | fetch | PUT `/api/v1/groups/:id/md` **or** `/api/v1/groups/:id/threads/:seg/md` (subchannel md, GroupMdEditor same save flow) returns 2xx (save group/subchannel markdown) | GET `/groups/:id/md` (view — `group_md_viewed` removed: panel render + post-edit readback re-pull ≠ view); non-2xx | None — per successful request | FetchRules.test.ts — it('GET /groups/:id/qrcode\|md\|incoming-webhooks 不产出 view/open 事件...') pins PUT `/md`→`group_md_edited`; it('子区 md 编辑...与群级同发 group_md_edited') |
| `group_member_added` | fetch | POST `/api/v1/groups/:id/members` returns 2xx (add member) | Non-2xx/cross-origin | None — per successful request | — (generic FETCH_RULES invariant only) |
| `group_member_removed` | fetch | DELETE `/api/v1/groups/:id/members` returns 2xx (remove member) | Non-2xx/cross-origin | None — per successful request | — (generic FETCH_RULES invariant only) |
| `group_name_edited` | body | PUT `/api/v1/groups/:id` **or** `/api/v1/groups/:id/threads/:seg` (subchannel rename, R11 🔴 roll-up) with top-level key `name` present on 2xx (rename group/subchannel) | body carrying `notice` instead (→ `group_announcement_edited`); `name` absent; non-JSON body | Presence-only; group-level `name`/`notice` mutually exclusive single-key update; per 2xx | BodyRules.test.ts — it('PUT /groups/:id name→改名, notice→改公告') + it('子区改名 PUT /groups/:g/threads/:t name→group_name_edited') |
| `group_nickname_edited` | fetch | PUT `/api/v1/groups/:id/members/:id` returns 2xx (edit member nickname) | Non-2xx/cross-origin | None — per successful request | — (generic FETCH_RULES invariant only) |
| `group_transferred` | fetch | POST `/api/v1/groups/:id/transfer/:id` returns 2xx (transfer group ownership) | Non-2xx/cross-origin | None — per successful request | — (generic FETCH_RULES invariant only) |
| `language_switched` | fetch | PUT `/api/v1/user/language` returns 2xx (change UI language) | Non-2xx/cross-origin | None — per successful request | — (generic FETCH_RULES invariant only) |
| `market_manual_publish_submitted` | fetch | POST `/market/api/v1/mcps` or POST `/market/api/v1/skills` returns 2xx (manually publish an MCP/skill) | Non-2xx; GET `/mcps\|skills/:id` (detail view) is pinned FETCH_IGNORE | None — per successful request | — (generic FETCH_RULES invariant only) |
| `market_skill_version_history_viewed` | fetch | GET `/market/api/v1/skills/:id/versions` returns 2xx (open skill version history) | GET `/skills/:id` (detail — FETCH_IGNORE, fetch layer can't tell view vs edit); non-2xx | Distinct segment count keeps it clear of the `/:id` IGNORE sentinel; per 2xx | FetchRules.test.ts — it('市场详情 GET /:id 不再产出 market_card_viewed...') asserts `skills/42/versions`→`market_skill_version_history_viewed` |
| `message_multiselect_deleted` | fetch | DELETE `/api/v1/message` returns 2xx (multi-select delete messages) | Non-2xx/cross-origin | None — per successful request | — (generic FETCH_RULES invariant only) |
| `subchannel_created` | imperative | `trackSubchannelCreated(...)` after a subchannel create succeeds — `createThreadByNameAndNotify` (顶栏, `source: channel_toolbar`) + `module.tsx` 右键创建 (`source: message_right_click`, carries `from_msg_type` via `inferMsgType` + `is_ai_msg` via `isMessageAuthorAi`) | Null/empty result (helper early-returns on falsy resp so a failed create never mis-fires); no `subchannel_id` derivable | Fires once per successful create; props `subchannel_id` / `source` / `title_len_bucket` / `channel_id`(父群,spec 关键属性;两个调用点各自传入;bare — stripSpacePrefix) (+ `from_msg_type` + `is_ai_msg` on the right-click path)。`is_ai_msg` = 源消息作者是否 AI/bot(`isMessageAuthorAi(message.fromUID)`,与 message_copied/forwarded/replied 同源判据;缓存读,作者 channelInfo 未拉取时退化为 false → 下限而非精确计数);顶栏路径无源消息 → 不发该字段(空值,非 false)。`subchannel_id` fail-open:`short_id ?? parseThreadChannelId ?? channel_id`,后两者落空时可能是复合 channel_id(`group____xxx`),不保证短 id | createThread.test.ts — trackSubchannelCreated 关键属性(channel_id/subchannel_id/source/title_len_bucket/from_msg_type/is_ai_msg)+ null-safety; (replaces the removed `message_subchannel_created` fetch rule — R? D1: POST `/groups/:id/threads` also fired by panel refresh → double-count) |
| `subchannel_opened` | imperative (S3, #1451) | 两入口:①`Pages/Chat` `componentDidMount` 当本页以子区频道(`ChannelTypeCommunityTopic`)挂载 = 从会话列表点子区行/文件预览 showConversation/深链恢复;②`componentDidUpdate` 当 `activeThread` 身份变化 = 页内 onOpenThreadPanel/onThreadSelect。决策抽到纯函数 `subchannelOpenTracking.ts`,props = `{channel_id: 父群 group_no(bare, stripSpacePrefix), subchannel_id: short_id}` | 文件预览等不改 activeThread 的更新;关闭子区(activeThread→null);挂载分支非 CommunityTopic;collection disabled | **两入口去重(R10 P1-1)**:入口②照发;若用户随后把该子区导航成完整视图/页内搜索/文件预览,会 remount 走入口①,此时三个导航调用点已把目标 channelID 写入 `WKApp.shared.pendingSubchannelOpenTracked` sentinel,挂载处命中同一 channelID 即跳过 → 一次开子区手势只发一次。直接从列表/深链挂载 sentinel 不命中,照常发。channel_id 归一 bare(见 channel_id 约定) | subchannelOpenTracking.test.ts(纯函数行为:两支发点/去重/strip);dapEventCoverage.pins.test.ts(会话列表子区行 DOM 门控) |
| `settings_secrets_configured` | fetch | POST `/api/v1/manager/secrets` (create) or PUT `/api/v1/manager/secrets/:id` (update) returns 2xx | Non-2xx/cross-origin | None — per successful request | — (generic FETCH_RULES invariant only) |
| `settings_secrets_deleted` | fetch | DELETE `/api/v1/manager/secrets/:id` returns 2xx (delete a saved secret) | Non-2xx/cross-origin; GET `/manager/secrets` list-load (that is `settings_secrets_opened`, imperative); POST/PUT (those are `settings_secrets_configured`) | None — per successful request | FetchRules.test.ts — it('DELETE /manager/secrets/:id → settings_secrets_deleted...') pins DELETE→event, keeps POST/PUT→configured, GET→undefined |
| `settings_secrets_opened` | imperative | SecretsSettingsPanel mount (useEffect []) = one open of the secrets manager | R12 🔴 P1-2 moved off `fetch`: GET `/api/v1/manager/secrets` is the panel's **list load**, re-pulled on mount, post-delete refresh, post-save (onSaved), and error-retry — one open + N edits would count as N+1 opens. POST/PUT manager/secrets (real write = configure) stay on fetch → settings_secrets_configured | mount-once (mirrors settings_voice_opened) | FetchRules.test.ts — it('GET /manager/secrets 不产出 settings_secrets_opened...') pins endpoint → undefined; it('相邻真实写仍各自命中') keeps POST/PUT→settings_secrets_configured; channelUniqueness self-check covers scan |
| `settings_voice_toggled` | imperative | `Dap.shared.track('settings_voice_toggled', { enabled })` in `settingsPages.tsx` after the settings-center voice state change or consent save succeeds | Must not fire when consent is cancelled or the state write fails | Fires only on a successful real toggle | `settingsPages.tsx` toggle/consent handlers |
| `space_join_new` | imperative | Emitted only on a genuine join (business status is neither NEED_APPROVAL nor PENDING): gated inside SpaceService.joinSpace (covers JoinSpaceModal/JoinSpacePage) + inline gated emits at the two direct-POST sites (apps/web Layout auto-join, InviteLanding), both after the approval early-return | R12 🔴 P1-4 moved off `fetch`: POST `/space/join` returns 2xx for **approval-gated** spaces where the user hasn't joined (status NEED_APPROVAL/PENDING = application submitted only). The path rule counts every 2xx, mis-recording "submit for approval" as "joined a space" — a 2xx≠business-success case the path channel can't express (same class as the summary envelope) | Fires once per genuine join; approval-pending submissions suppressed by the status gate | FetchRules.test.ts — it('POST /space/join 不产出 space_join_new...') pins endpoint → undefined; channelUniqueness self-check covers scan |
| `user_login` | fetch | POST `/api/v1/user/login` or `/api/v1/user/emaillogin` returns 2xx (successful login) | 4xx/5xx (action didn't happen — dedicated negative test); `app_launched` not on this channel | None — per successful request | Dap.test.ts — it('2xx 的第一方请求既发 http_request 又补发映射事件 (POST /api/v1/user/login → user_login)'); paired it('4xx 不补发映射事件...') |
| `user_logout` | fetch | POST `/v1/auth/oidc/:seg/logout` returns 2xx (user logs out) | Non-2xx/cross-origin | None — per successful request | — (generic FETCH_RULES invariant only; oidcLogout.test.ts covers flow, not the telemetry map) |
| `webhook_created` | fetch | POST `/api/v1/groups/:id/incoming-webhooks` **or** `/api/v1/groups/:id/threads/:seg/incoming-webhooks` (subchannel scope) returns 2xx (create incoming webhook) | GET `/groups/:id/incoming-webhooks` (list readback after add/reset/delete — `group_webhook_panel_opened` removed); non-2xx | None — per successful request | FetchRules.test.ts — it('GET /groups/:id/qrcode\|md\|incoming-webhooks 不产出 view/open 事件...') pins POST→`webhook_created`; it('子区 thread 作用域 webhook 与群级同事件') pins thread-nested paths |
| `webhook_deleted` | fetch | DELETE `/api/v1/groups/:id/incoming-webhooks/:id` **or** `/api/v1/groups/:id/threads/:seg/incoming-webhooks/:id` returns 2xx (delete webhook) | Non-2xx/cross-origin | None — per successful request | FetchRules.test.ts — it('子区 thread 作用域 webhook 与群级同事件') |
| `webhook_edited` | body | PUT `/api/v1/groups/:id/incoming-webhooks/:id` **or** `/api/v1/groups/:id/threads/:seg/incoming-webhooks/:id` (subchannel scope) with an edit payload — name/avatar/allow_mention_*/mention_uids, no top-level `status` key — on 2xx (fallback branch of the webhook PUT rule) | body carrying `status` (→ `webhook_enabled_toggled`); non-2xx/cross-origin; non-JSON body | Discriminator order: `status` present → toggle, else `webhook_edited` fallback; per 2xx | BodyRules.test.ts — it('PUT incoming-webhooks/:id: 有 status→启停, 否则→编辑(fallback)'); it('子区 thread 作用域 webhook PUT 同样区分启停/编辑') |
| `webhook_enabled_toggled` | body | PUT `/api/v1/groups/:id/incoming-webhooks/:id` **or** `/api/v1/groups/:id/threads/:seg/incoming-webhooks/:id` (subchannel scope) with top-level key `status` present on 2xx (enable/disable webhook via updateStatus) | edit payload (name/avatar/allow_mention_*/mention_uids, never carries `status`) → falls through to `webhook_edited` fallback; non-JSON body | Discriminator order: `status` present → toggle, else `webhook_edited` fallback (disambiguates toggle vs edit); presence-only `status` so both enable/disable count; per 2xx | BodyRules.test.ts — it('PUT incoming-webhooks/:id: 有 status→启停, 否则→编辑(fallback)'); it('子区 thread 作用域 webhook PUT 同样区分启停/编辑') |
| `webhook_tested` | fetch | POST `/api/v1/groups/:id/incoming-webhooks/:id/test` **or** `/api/v1/groups/:id/threads/:seg/incoming-webhooks/:id/test` returns 2xx (send test webhook) | Non-2xx/cross-origin | None — per successful request | FetchRules.test.ts — it('子区 thread 作用域 webhook 与群级同事件') |
| `webhook_url_reset` | fetch | POST `/api/v1/groups/:id/incoming-webhooks/:id/regenerate` **or** `/api/v1/groups/:id/threads/:seg/incoming-webhooks/:id/regenerate` returns 2xx (regenerate webhook URL) | Non-2xx/cross-origin | None — per successful request | FetchRules.test.ts — it('子区 thread 作用域 webhook 与群级同事件') |


## 2. `track(testid)` + `data-track` channels (DOM delegation)

| Event | Channel | Fires exactly when | Must NOT fire (negative/failure edge) | Reset/dedup | Pinning test |
|---|---|---|---|---|---|
| `channel_opened` | imperative(两处:`ConversationList` 行 onClick + `Contacts` GroupCard `onEnterChat`) | ①会话列表:点会话行打开频道 —— 行 onClick 调 `_trackChannelOpened`,compact + flat 两渲染路径;payload 决策抽到纯函数 `channelOpenedTracking.ts`。**子区行(`ChannelTypeCommunityTopic`)不发本事件**——helper 对子区返回 null,改由 `subchannel_opened` 命令式覆盖。②通讯录:群聊名片「进入聊天」(`GroupCard.onEnterChat`,`Contacts/index.tsx`)命令式发同名事件。props = `{object_id: 原始 channelID(raw), channel_type: person\|group\|other, is_ai?}`;`is_ai` **仅 person(私聊)行携带**:派生抽到纯函数 `resolveAiPeer`——先看**带前缀** `channelInfo.orgData.robot===1`(与行内 AiBadge 同 key、同口径),否则以 **`stripSpacePrefix(channelID)` 后的裸 uid** 走 `isMessageAuthorAi`(octoAssistantUids + SYSTEM_BOTS)。**必须 strip**:Space 部署 Person channelID 形如 `s<32hex>_<uid>`,不 strip 则两个 uid-list 判据恒 false、助手 DM(浓度分子)被系统性误标 is_ai:false(与兄弟事件 `octo_assistant_opened` 同修法);缓存未拉到仍退化 false → 下限非精确。群 / 其他及通讯录群聊路径不带 is_ai(空值非 false)。**私聊 AI 浓度 = is_ai=true 数 / channel_type=person 数** | 子区行(→`subchannel_opened`);compact 行内拖拽柄 / 展开子区标签(仍 `e.stopPropagation()`,冒泡到行 onClick 前即止 → 不触发采集) | 点击即发,无跨事件去重(与旧 data-track 语义一致);两处 emit 运行时互斥:通讯录路径经 `GroupCard→showConversation`,**从不点 ConversationList 行**,同一「开会话」手势只发一次,无 double-count → channel_opened 现为**单一命令式通道**(消除原 data-track+imperative 双通道例外)。`object_id`=原始 channelID(**raw,遗留口径**:本事件已上线,不做 stripSpacePrefix 以免二次改变已发布指标序列;查询侧 join 自行 strip,见开头 channel_id 约定)。**⚠ 通道迁移(本 PR)**:data-track→命令式并新增 is_ai/channel_type;触发时机(点击即发)与 object_id 口径均不变 → 已发布 channel_opened 序列连续、无断点。**⚠ 等价性前提(review P2-2)**:旧 data-track 走**捕获阶段**委托(`data-track-ignore` 驱动排除),新路径走行根**冒泡** onClick(子元素 `stopPropagation` 驱动排除);二者当前 emit 集恰好一致,**仅因唯二的非打开子元素(拖拽柄、展开子区标签)都 stopPropagation**——将来出现"stop 而无 ignore / ignore 而无 stop"的子元素会静默移动本指标。行根未挂 tabIndex/role,键盘路径无回归。pins 测试钉住这两处 stopPropagation 仍在 | channelOpenedTracking.test.ts(payload / channel_type / 子区 null / raw object_id;**resolveAiPeer:robot 短路 + Space 前缀 strip 后裸 uid 查判据**);dapEventCoverage.pins.test.ts(无 data-track + 两处 onClick 均接 _trackChannelOpened + 两处 data-object-id 保留 + 两处 stopPropagation + 通讯录发点) |
| `channel_search_opened` | track(testid) — `channel-search-entry` | User clicks the channel-search entry control → opens search | Ancestor with `data-track` (wins, table not consulted); keydown/submit (`on:'click'`); never reads value/innerText | `on:'click'` one-shot open, not a toggle → no double-fire | — |
| `channel_summary_panel_opened` | track(testid) — `summary-chat-panel-header-btn` | User clicks the summary chat-panel header button → opens summary panel | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot | TrackRules.rules.test.ts — `has a click rule $event → $testid` (+ `every A_rule testid is indexed under byTestid`) |
| `group_admin_add_dialog_opened` | track(testid) — `group-add-manager-btn` | User clicks the add-manager button → opens admin-add dialog | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot dialog open | — |
| `group_bot_admin_add_dialog_opened` | track(testid) — `group-add-bot-admin-btn` | User clicks the add-bot-admin button → opens bot-admin-add dialog | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot | — |
| `group_bot_admin_remove_dialog_opened` | track(testid) — `group-bot-admin-remove-btn` | User clicks the bot-admin remove button → opens remove dialog | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot | — |
| `group_dissolve_dialog_opened` | track(testid) — `group-disband-btn` | User clicks the disband/dissolve-group button → opens dissolve dialog | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot | — |
| `group_md_preview_toggled` | track(testid) — `group-md-preview-btn` | User clicks the markdown-preview toggle button (each click = one toggle, both directions counted) | Ancestor `data-track`; keydown/submit | `on:'click'`; unlike removed open-only toggles this event intentionally counts every toggle → no gating needed | — |
| `group_member_add_dialog_opened` | track(testid) — `group-member-add-btn` | User clicks the add-member button → opens member-add dialog | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot | — |
| `group_member_remove_dialog_opened` | track(testid) — `group-member-remove-btn` | User clicks the remove-member button → opens member-remove dialog | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot | — |
| `input_attachment_clicked` | track(testid) — `input-attachment-btn` | User clicks the attachment button in the message input | Ancestor `data-track`; keydown/submit | `on:'click'` | — |
| `input_sticker_sent` | track(testid) — `input-sticker-item` | User clicks a sticker item to send it | Ancestor `data-track`; keydown/submit; never reads sticker value/text | `on:'click'` | — |
| `market_bot_publish_prompt_copied` | imperative — shared `PromptForwardActions.copyTrackEvent`, passed as `"market_bot_publish_prompt_copied"` by `McpBotPublishModal` and skill `BotPublishModal`; fires only after `copyToClipboard` resolves true | User copies the bot-publish prompt (skill 上架 at `/mcp-market/skills`, MCP 上架 at `/mcp-market/mine`) | On click before the copy resolves; permission-denied / non-secure context; the expert/squad publish surface (passes no `copyTrackEvent`) | Per-caller event prop on the shared component, NOT a route gate — the old `/mcp-market/mcp` gate no longer matched either surface after the restructure (feat/market-ui-restructure) | — |
| `market_card_opened` | data-track (McpCard root div, SkillCard `<article>` root; also list-page wrappers) | User clicks OR Enter/Space keyboard-activates the card root (role=button) → opens detail modal | Clicks inside the `data-track-ignore` footer (edit/delete/stats); non-activation keys (a/Tab/Arrow); keydown on a native child button inside the card (browser synthesizes the button's own click); the install button (own `data-track`, closest() stops at it) | Capture-phase delegate resolves nearest `[data-track]`; footer `data-track-ignore` excludes; keyboard fallback skips native controls so no double-count | Dap.trackIgnore.test.ts — `market card: a click inside the data-track-ignore footer does NOT emit market_card_opened, but the install button keeps its own event`; Dap.keyboard.test.ts — `emits market_card_opened on Enter/Space over a non-native role=button card` (+ `does NOT emit on non-activation keys`) |
| `market_publish_entry_clicked` | track(testid) — `mcp-publish-entry`, `skill-publish-entry` | User clicks the publish-entry control on the MCP or Skill market | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot | — |
| `market_publish_method_selected` | track(testid) — `mcp-publish-method-bot`/`-manual`, `skill-publish-method-bot`/`-manual` (props `method:'bot'\|'manual'`) | User clicks a publish-method option (bot or manual) in the MCP or Skill publish flow | Ancestor `data-track`; keydown/submit | Static enum prop `method` distinguishes the four; `on:'click'` | — |
| `market_skill_install_clicked` | data-track (SkillCard footer Install `<button>`) | User clicks the Install button on a skill card | Card-open (`market_card_opened`) is NOT logged for this click — button carries its own `data-track` so closest() stops at it, shielding it from the footer's `data-track-ignore` and the card ancestor | Own `data-track` shields from ancestor `market_card_opened` even inside the ignore footer | — (behavior exercised generically with stand-in name `skill_install_clicked` in Dap.trackIgnore/Dap.keyboard; real event name not pinned) |
| `market_skill_sorted` | imperative (prop `sort_by`) | `SkillListPage.handleSortChange(next)` — user picks a sort option in the skill market; only when `next !== sort` (re-picking the current sort returns early) | Re-selecting the active sort (same-value guard); originally a per-option `onClick` + `skill-sort-option` DOM-delegate rule that double-counted clicks on the already-active sort | Same-value guard replaces the unconditional per-item delegate; `sort_by` prop is a fixed enum (renamed from `sort` for schema alignment, #1451; no privacy change) — 八审 P2 | — |
| `message_copied` | imperative (copy menu `onClick`) | User clicks the "Copy" item in the message context menu; props = `{object_id, message_id, is_ai_msg}` (`is_ai_msg` = 被复制消息作者是否 AI/bot,`isMessageAuthorAi(message.fromUID)` 同源判据;缓存读,作者 channelInfo 未拉取时退化为 false → 该属性是下限而非精确计数) | Non-text/richText messages (menu not shown); never reads message body/innerText | migrated from `ctx-message-copy` DOM `data-track` so it can carry `is_ai_msg` (session.go ai_msg_copy) — #1452 | — |
| `message_forward_panel_opened` | track(testid) — `ctx-message-forward` | User clicks the "Forward" item in the message context menu → opens forward panel | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot | — |
| `message_multiselect_delete_dialog_opened` | track(testid) — `multiselect-delete-btn` | User clicks the delete button in multiselect mode → opens delete dialog | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot | — |
| `message_multiselect_forward_panel_opened` | track(testid) — `multiselect-forward-btn`, `multiselect-mergeforward-btn` | User clicks the multiselect forward OR merge-forward button → opens forward panel (one event, two testids) | Ancestor `data-track`; keydown/submit | `on:'click'`; both testids map to same event | — |
| `message_multiselect_started` | track(testid) — `ctx-message-multiselect` | User clicks "Select/Multiselect" in the message context menu → enters multiselect | Ancestor `data-track`; keydown/submit | `on:'click'` | — |
| `message_subchannel_create_dialog_opened` | ~~track(testid) — `ctx-message-create-thread`~~ **removed** | 右键「创建子区」的 dialog-open 已并入 `channel_subchannel_create_dialog_opened`(顶栏 + 右键同名)。原 DOM 规则删除,避免同手势双记不同名(#1452 review P1) | — | 已移除 | — |
| `my_info_opened` | track(testid) — `nav-user-avatar` | User clicks the nav-rail user avatar → opens own-info | Ancestor `data-track`; keydown/submit | `on:'click'` | — |
| `nav_tab_switched` | data-track (`NavItem` native `<button>`) | User clicks a nav-rail item → switches tab | keydown delegate skips native `<button>` (browser-synthesized click covers activation) → no double-count; no `data-track-ignore` subtree here | Native button single-fire (keydown fallback skips native controls); `data-object-id`=routePath | — (native-button no-double-count behavior covered generically with stand-in name in Dap.keyboard) |
| `onboarding_about_clicked` | track(testid) — `onboarding-about-link` | User clicks the "About" link in onboarding | Ancestor `data-track`; keydown/submit | `on:'click'` | — |
| `onboarding_opensource_clicked` | track(testid) — `onboarding-opensource-link` | User clicks the "Open source" link in onboarding | Ancestor `data-track`; keydown/submit | `on:'click'` | — |
| `settings_notification_toggled` | track(testid) — `nav-settings-notification-toggle` | User clicks the notification toggle in settings (each click = one toggle, both directions counted) | Ancestor `data-track`; keydown/submit | `on:'click'`; intentionally counts every toggle direction → no open-only gating | — |
| `settings_onboarding_guide_reopened` | track(testid) — `nav-settings-onboarding` | User clicks the "reopen onboarding guide" item in settings | Ancestor `data-track`; keydown/submit | `on:'click'` | — |
| `smart_summary_agent_new_session` | track(testid) — `summary-agent-new-session-btn` | User clicks the "new session" button in the summary agent panel | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot | TrackRules.rules.test.ts — `has a click rule $event → $testid` (+ byTestid-index assertion) |
| `smart_summary_delete_dialog_opened` | track(testid) — `summary-detail-delete-btn` | User clicks the delete button in summary detail → opens delete dialog | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot | TrackRules.rules.test.ts — `has a click rule $event → $testid` (+ byTestid-index assertion) |
| `smart_summary_edit_opened` | track(testid) — `summary-detail-edit-btn` | User clicks the edit button in summary detail → opens edit | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot | TrackRules.rules.test.ts — `has a click rule $event → $testid` (+ byTestid-index assertion) |
| `smart_summary_regenerate_dialog_opened` | track(testid) — `summary-detail-regenerate-btn` | User clicks the regenerate button in summary detail → opens regenerate dialog | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot | TrackRules.rules.test.ts — `has a click rule $event → $testid` (+ byTestid-index assertion) |
| `webhook_create_dialog_opened` | track(testid) — `webhook-create-btn` | User clicks the create-webhook button → opens create dialog | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot | — |
| `webhook_edit_dialog_opened` | track(testid) — `webhook-edit-btn` | User clicks the edit-webhook button → opens edit dialog | Ancestor `data-track`; keydown/submit | `on:'click'` one-shot | — |


## 3. `imperative` / `helper` — smart-summary & IM

| Event | Channel | Fires exactly when | Must NOT fire (negative/failure edge) | Reset/dedup | Pinning test |
|---|---|---|---|---|---|
| `ai_mentioned` | helper | On sendack Normal (reasonCode===1) via `trackMessageSent`, when consumed intent had `mentionAis`/`mentionedBots` — one event per @-ed AI bot (bot_id/bot_type), degrading to one bot-less event when the bot list is unresolved | No intent for the clientSeq (e.g. forward path); collection disabled (fail-closed); sendack reasonCode≠1 | Rides `message_sent`'s intent consume (delete on read → once per clientSeq); intents cleared on kill switch | trackMessage.globalAck.test.ts `it('emits ai_mentioned per @-ed bot alongside message_sent')` |
| `channel_create_started` | imperative | Clicking the empty-state "start group" button, which invokes `groupMenu.onClick` — this button has no `data-track`, so the imperative call mirrors the bubble's declarative start-group item | Bubble start-group path (already covered by its `data-track`; two entries mutually exclusive, no double-count); `groupMenu?.onClick` absent | Mutual-exclusion with declarative rule | — |
| `channel_search_filter_panel_opened` | imperative | `toggleFilterOpen` on the open edge only (`!filterOpen`) | Close edge (old TrackRules toggle rule counted open+close → doubled; removed) | Open-edge only | FetchRules.test.ts `it('这些「UI 采集专属」事件名不得再出现在 path 通道规则表里')` (name banned from path channel) |
| `channel_search_tab_switched` | imperative | `onTabChange` when the tab actually changes (`tab && tab!==activeTab`); props `{ tab }` | Same-tab re-select; search/debounce/paging re-fire (old POST `_search_media`/`_search_files` 2xx rule over-counted; removed) | Fires only on a real switch | FetchRules.test.ts uiOnly-path-ban `it('这些「UI 采集专属」事件名不得再出现在 path 通道规则表里')` |
| `channel_subchannel_panel_opened` | imperative | Clicking the subchannel header on a group channel when the thread list is currently NOT visible (open edge) | Close edge; file-preview reusing ThreadPanel; dead `ThreadList.componentDidMount`; GET `/groups/:id/threads` refresh re-fire (all removed/avoided) | Open-edge only | FetchRules.test.ts uiOnly-path-ban `it('这些「UI 采集专属」事件名不得再出现在 path 通道规则表里')` |
| `channel_search_result_clicked` | imperative (S3, #1451) | `ChannelSearchPanel` — user clicks a search result row to jump to the message | Non-click UI (hover/scroll); empty result set | Fires per result click | — (new; schema owner to confirm registration) |
| `channel_search_query` | imperative (S3, #1451) | `ChannelSearchPanel` → `useSearchPagination` `onQueryStart`,首页检索执行**且带 keyword 或有效 filter**才计(媒体/文件 tab 的 shouldRunSearch 恒 true,仅切 tab 会触发空首页请求,故 gate 掉,与 `channel_search_tab_switched` 分开);props = `{channel_id, tab}`,`channel_id` 经 `stripSpacePrefix` 归一为 bare(为跨事件数仓 join,**非**与后端 `_search_` 对齐——`SearchService` 发 raw channelID,见 channel_id 约定)。**绝不上报关键词** | 翻页(带 cursor)不计;空 keyword+无 filter 的浏览 tab 首页加载;`canSearch`=false / 输入法组字中;collection disabled | 每次有意义首页检索 one-shot(debounce 后);判别在 keyword 值/嵌套 filters(值级、被 sanitize 拦)→ 退命令式(见 BodyRules.ts) | dapEventCoverage.pins.test.ts;useSearchPagination.test.tsx(`onQueryStart fires on first-page search, not on pagination`) |
| `channel_subchannel_create_dialog_opened` | imperative (S3, #1451) | 打开「创建子区」确认弹窗的两个入口统一到本事件:`ThreadPanel.handleCreateThread`(顶栏)+ `module.tsx` 消息右键菜单「创建子区」onClick(右键)。原右键 DOM 规则 `message_subchannel_create_dialog_opened` 已删除,同手势只发本事件一次 | Dialog close/cancel | Open-edge only(两入口各自 one-shot;已无同名/异名重复) | — (new; schema owner to confirm registration) |
| `conversation_leave_dialog_opened` | imperative | Opening the leave/exit confirm dialog in the channel-setting danger section | Owner branch (early `return` before track, blocked) | — | — |
| `input_emoji_picker_opened` | imperative | `togglePanel` open branch (else of `state.show`) | Close branch (old input-emoji-btn toggle rule counted close as open → doubled; removed) | Open-edge only | FetchRules.test.ts uiOnly-path-ban `it('这些「UI 采集专属」事件名不得再出现在 path 通道规则表里')` |
| `input_expanded` | imperative | `toggleExpand` when `next===true` (expanding) | Collapsing branch (old input-expand-btn toggle rule doubled; removed) | Expand-edge only | FetchRules.test.ts uiOnly-path-ban `it('这些「UI 采集专属」事件名不得再出现在 path 通道规则表里')` |
| `input_mention_ai_selected` | imperative | `selectItem` when the chosen item `isBot`; props empty (no bot name/uid) | Selecting a non-bot mention | — | — |
| `input_mention_opened` | imperative | Mention suggestion `onStart` when candidate items exist; props empty | `onStart` with no items (early return) | — | — |
| `input_voice_recording_started` | imperative | Effect when `isRecording` truly starts; props empty | Cancel-pending path (early return before track) | — | — |
| `message_context_menu_opened` | imperative | Native right-click `contextmenu` handler fires (bypasses delegation mask); props empty | — | — | — |
| `message_forwarded` | imperative | After `ForwardService.send` when result kind !== "all-failed"; props = `{object_id, message_id, is_ai_msg}` (`is_ai_msg` = 被转发消息作者是否 AI/bot,`isMessageAuthorAi(message.fromUID)`;缓存读,作者 channelInfo 未拉取时退化为 false → 下限而非精确计数) | All targets failed (all-failed, e.g. group disbanded); build-content throw (catch) | is_ai_msg added for session.go ai_msg_forward — #1452 | — |
| `message_multiselect_forwarded` | imperative | Multiselect forward — two entries (multi-forward and merge-forward) — when kind !== "all-failed" | All-failed; build-content throw (catch) | — | — |
| `message_replied` | helper | Inside `trackMessageSent` (sendack Normal) when the consumed intent has `isReply`; props = `{object_id, message_id, channel_id, actor_type:'user', is_ai_msg}` (spec 关键属性 {is_ai_msg, channel_id, actor_type}; `actor_type` sink 口径 user/bot,不用 human; `is_ai_msg` = 被回复消息作者是否 AI/bot,由 VM 生产者查 subscriber robot 标记注入;无正文/引用内容) | Non-reply send; no intent; collection disabled | Rides `message_sent`'s once-per-clientSeq consume | trackMessage.globalAck.test.ts — it('emits message_replied with spec 关键属性 ...') + is_ai_msg 缺省 false |
| `message_revoked` | helper | After revoke success via single-channel `trackMessageRevoked`; one event with `channel_type` + `object_id` | Fetch rule POST `/message/revoke` and dead bubble `onMessageRevoke` (both removed — would double-count) | Single channel; `object_id` null when clientSeq missing (no content leak) | trackMessage.globalAck.test.ts `it('emits exactly one message_revoked with rich props per revoke')`, `it('object_id is null when clientSeq is missing (no content leak)')` |
| `message_sent` | helper | VM-independent global sendack listener, reasonCode===1 (submitted quality), consuming the intent by clientSeq; `object_id`=clientSeq; `message_id`=`String(SendackPacket.messageID)`(服务端分配 ID,sendack 才拿得到;取不到时 sanitizeProps 丢弃) | No intent (forward path); reasonCode≠1; collection disabled (fail-closed, no listener bound, no-op); duplicate sendack | `intents.delete` on consume → exactly once per clientSeq; `onDisabled` clears intents; MAX_INTENTS=500 cap | trackMessage.globalAck.test.ts `it('emits message_sent from the global listener even when no sending VM is present')`, `it('carries message_id from the sendack messageID')`, `it('consumes each intent once — a duplicate sendack does not double-count')`, `it('ignores non-accepted sendack (reasonCode !== 1)')` |
| `smart_summary_agent_message_sent` | imperative | `handleSend`: stream path fires inside `startSSEStream` AFTER the `!profile` guard (post-success); non-stream path fires in-place after `onSend`. Single channel covering click + Enter | `!profile` guard rejects the SSE send (Toast only, no send, no track); old summary-agent-send-btn click rule (missed Enter) and POST `/agent/chat` 2xx rule (SSE-fallback double-count) — both removed | Single channel | FetchRules.test.ts uiOnly-path-ban `it('这些「UI 采集专属」事件名不得再出现在 path 通道规则表里')` (no positive AgentChatPanel track assertion) |
| `smart_summary_agent_reference_added` | imperative | `handleSelect` — choosing a historical summary as a reference in agent mode; props empty | — | — | — |
| `smart_summary_completed` | imperative (prop `result`) | `trackSummaryCompletedOnce` when status reaches any terminal state (COMPLETED/FAILED/CANCELLED) and `completedTrackedTaskId !== taskId` (shared by status-event and fallback-poll paths); `result` prop carries `completed`/`failed`/`cancelled` | Second path observing the same non-terminal→terminal transition (id-anchor skip); status still non-terminal | Anchor set on emit; cleared when task leaves the terminal set (regenerate→PENDING) so re-complete counts again; per-task not global | SummaryDetailPage.schedule.test.tsx `it('emits smart_summary_completed once when both paths observe the same pending→completed transition')`, `it('re-tracks for a different task id (dedup is per-task, not global)')`, `it('re-tracks the SAME task after regenerate ...')` |
| `smart_summary_create_clicked` | imperative | `handleCreate` — any of the three create controls funnel here | Old GET `/summary-templates` page-load inference (removed) | — | FetchRules.test.ts uiOnly-path-ban `it('这些「UI 采集专属」事件名不得再出现在 path 通道规则表里')` |
| `smart_summary_custom_template_created` | helper | `createCustomTopicTemplate` (POST `/summary-templates/my`) when the response envelope `code===0` (`trackOnEnvelopeSuccess`); props empty | `code!==0` / missing code (logical failure, gateway envelope) | Single api-layer sink; `code===0` gate | — |
| `smart_summary_deleted` | helper | `deleteSummary` (DELETE `/summaries/:taskId`) when the response envelope `code===0`; props empty | `code!==0` / missing code (logical failure) | Api-layer sink; `code===0` gate | — |
| `smart_summary_edited` | helper | `editSummary` (PUT `/summaries/:taskId/edit`) when the response envelope `code===0` (via `trackOnEnvelopeSuccess`); props empty | `code!==0` / missing code (logical failure) | Api-layer sink; `code===0` gate | — |
| `smart_summary_forward_panel_opened` | imperative | `handleForwardToChat` opening the conversation-select panel, only when `sourceContent` is non-empty | Empty/whitespace content (early return before track) | — | — |
| `smart_summary_forwarded` | imperative | After `ForwardService.send` in forward-to-chat when `state.kind !== "all-failed"`; props always empty | All-failed | — | — |
| `smart_summary_member_added` | helper | `addMembers` (POST `/summaries/:taskId/members`) when the response envelope `code===0`; props empty | `code!==0` / missing code (logical failure) | Api-layer sink; `code===0` gate | summaryApi.test.ts — DAP-110 Stage 2 gate: emits once with empty props on code===0; not on code!==0 / 缺 code |
| `smart_summary_member_removed` | helper | `removeMember` (creator removes a member, DELETE `/summaries/:taskId/members?uid=`) when envelope `code===0`; props empty. Participant self-leave (`leaveSummary`) is a distinct action with no registered event and is deliberately NOT tracked here | `code!==0` / missing code; participant self-leave | Api-layer sink; `code===0` gate | summaryApi.test.ts — DAP-110 Stage 2 gate |
| `smart_summary_mode_switched` | imperative | `SummaryListPage.handleCreate(mode)` when `mode==="agent"` — 列表页「+」下拉显式选择 Agent 总结（创建页内切换入口已随智能总结模式选择上移移除）；props `{to:"agent"}` | normal 入口（create_clicked 即可，不视为切换） | — | — |
| `smart_summary_module_entered` | imperative | NavRail "summary" top entry `onPress` when `!reentry` | Re-clicking the already-active summary menu (`reentry`, host passes prevMenuId===id) | reentry guard | — |
| `smart_summary_opened` | imperative | SummaryCard `onClick` opening detail (dynamic testid can't use delegation) | — | — | — |
| `smart_summary_quality_gate` | imperative | After an Agent summary task has been created successfully, `trackAgentSummaryQuality` emits from the unified workbench save path (`SummaryWorkbenchFeature.savePreview`) and the legacy Agent save path (`SummaryCreatePage.handleSaveAsSummary`). Props are bounded to `task_id`, `finish_status` (`unreported` when the optional verdict is absent), `gap_count`, optional `first_gap_kind`, and existing context dimensions such as `object_id`, `source`, `entry_point`, `entry_source`, and `trigger_mode` | Transport, protocol, or task-creation/save failure; never carries `gaps[].detail`, summary content, keywords, or other user content | Workbench: `handledSavedTaskIds` dedupes by `task_id`; legacy: one emission in the successful save lifecycle before session teardown | SummaryWorkbenchFeature.test.tsx — owner-confirmed PARTIAL/FAILED save-success cases, missing-verdict case, `gap.detail` negative assertion, and recovered-result dedup; SummaryCreatePage.test.tsx — owner-confirmed PARTIAL/FAILED save-success and missing-verdict cases |
| `smart_summary_regenerated` | helper | `regenerateSummary` (POST `/summaries/:taskId/regenerate`, by-group) **and** `regeneratePersonalSummary` (POST `/summaries/:taskId/personal-regenerate`, by-person) when the response envelope `code===0` — both are the SAME full-regenerate funnel step, so both feed one event (八审 P2 补 by-person 分支,否则 dialog_opened→regenerated 漏斗在 by-person 侧断裂) | `code!==0` / missing code (logical failure). NOT refine-by-feedback — that is a semantically distinct "按反馈微调" action, deliberately excluded from this full-regenerate funnel | Api-layer sink; `code===0` gate | — |
| `smart_summary_scope_channel_selected` | imperative | `handleToggle` add branch (selecting a channel) | Deselect (uncheck); old GET `/summary-chat-candidates` list-load inference (removed) | Add-edge only | FetchRules.test.ts uiOnly-path-ban `it('这些「UI 采集专属」事件名不得再出现在 path 通道规则表里')` |
| `smart_summary_scope_participant_selected` | imperative | `handleToggle` add branch (selecting a member) | Deselect (uncheck); old GET `/summary-member-candidates` list-load inference (removed) | Add-edge only | FetchRules.test.ts uiOnly-path-ban `it('这些「UI 采集专属」事件名不得再出现在 path 通道规则表里')` |
| `smart_summary_searched` | imperative | After 400ms debounce in `handleKeywordChange`, only when `value.trim()` non-empty; keyword value never collected | Empty/whitespace keyword | Debounce timer | — |
| `smart_summary_started` | helper | `createSummary`/`createAgentSummary` — emitted only in the api layer when response envelope `code===0` (`trackOnEnvelopeSuccess`), once, with caller-supplied props; PLUS a component-level sink: `SummaryWorkbenchFeature.notifyCreated` tracks `smart_summary_started` once per taskId (`notifiedTaskIds` dedupe) when a workbench-created preview is saved or a workbench workflow is created | `code!==0` (HTTP200 + logical failure); `code===null` or MISSING code (gateway envelope, same failure signature); agent business failure | Two sinks by design (api layer for legacy create entries; workbench component for the unified workbench entry) — deduped per taskId so one action cannot double-emit; no createSummary/createAgentSummary call exists in the workbench tree | summaryApi.test.ts `it('emits once with the caller props when envelope code===0')`, `it('does NOT emit when envelope code!==0 (HTTP200 + 逻辑失败)')`, `it('does NOT emit when envelope code===null (空/网关信封,P2-5)')`, `it('does NOT emit when envelope code 缺省...六审 P2')`, `it('agent mode emits once after envelope success (P2-2)')`, `it('agent mode does NOT emit on business failure (code!==0)')` |
| `smart_summary_status_filtered` | imperative | `handleStatusChange` — status-filter dropdown change; status value not collected | — | — | — |
| `smart_summary_task_cancelled` | helper | `cancelSummary` (POST `/summaries/:taskId/cancel`) when the response envelope `code===0`; props empty | `code!==0` / missing code (logical failure) | Api-layer sink; `code===0` gate | summaryApi.test.ts — DAP-110 Stage 2 gate |
| `smart_summary_template_applied` | imperative | `handleTemplateClick` — built-in and custom template cards both funnel here; PLUS `SummaryWorkbenchFeature.applyTemplate` (unified workbench entry), payload empty | — | — | — |
| `smart_summary_theme_input` | imperative | Topic textarea `onChange` after 600ms debounce, only when non-empty; content never collected; PLUS workbench composer `onInputChange` after 600ms debounce (SummaryWorkbenchFeature, timer cleared on unmount) | Empty topic; fire after unmount (debounce timer cleared on unmount); fire after submit (timer cleared in `handleSubmit` — user已进入生成,八审 P2) | Debounce timer reset per keystroke, cleared on unmount and on submit | — |
| `smart_summary_timer_configured` | helper | `createSummarySchedule` (POST `/summary-schedules`) and `updateSummarySchedule` (PUT `/summary-schedules/:id`) when the response envelope `code===0`; both create+edit of a scheduled summary funnel here | `code!==0` / missing code (logical failure) | Api-layer sink; `code===0` gate | — |
| `smart_summary_timer_dialog_opened` | imperative | `openScheduleModal` — opening the scheduled-summary config dialog; props empty | — | — | — |
| `smart_summary_version_restored` | helper | `restoreSummaryVersion` (POST `/summaries/:taskId/versions/:resultId/restore`, by-group) **and** `restorePersonalSummaryVersion` (POST `/summaries/:taskId/personal-versions/:versionId/restore`, by-person) when the response envelope `code===0` — both scopes of the same restore action feed one event (mirrors `smart_summary_regenerated` by-group/by-person) | `code!==0` / missing code (logical failure) | Api-layer sink; `code===0` gate | summaryApi.test.ts — DAP-110 Stage 2 gate (both team + personal) |
| `thread_expanded` | imperative | Fold-session toggle `onClick` when `wasExpanded===false` (expanding); props empty | Collapsing (wasExpanded true) | Expand-edge only | — |


## 4. `imperative` / `helper` — marketplace, bot & app lifecycle

| Event | Channel | Fires exactly when | Must NOT fire (negative/failure edge) | Reset/dedup | Pinning test |
|---|---|---|---|---|---|
| `app_launched` | imperative | `maybeTrackLaunch()` runs at top of `Dap.track()` and `Dap.pageView()`; fires exactly once when tracking is enabled AND `currentToken()` is truthy (logged in) — i.e. the first real event after login | While disabled (`!enabled`); enabled-but-no-token (anonymous login page — firing there would write a persistent device_id + a tokenless POST for anonymous visitors); a second time | `launchTracked` sentinel → whole lifecycle only once (disable→re-enable does not repeat); NO LONGER fired in `setEnabled(true)` — deferred so setEnabled on the pre-login page can't fire it | `dmworkbase/src/Service/__tests__/Dap.test.ts` describe "Dap — app_launched 延后至登录后(六审 P2 / owner 决策 b)"; it '登录拿到 token 后:首个事件触发 app_launched 一次,且排在该事件之前' (+ 'enabled 但无 token(匿名登录页):不发…' + '整生命周期仅一次:第二个事件不再补发') |
| `app_opened` | imperative (S3, #1451) | `openAppBotConversation` else-branch — user opens an app-bot conversation whose uid is NOT in `WKApp.remoteConfig.octoAssistantUids`; props `app_name`=displayName, `app_category`=scope | uid IS an Octo assistant (→ `octo_assistant_opened`) | Fires once per open from the app-bot list (`source: app_bot_list`) | appBotConversation.test.tsx `it('tracks app_opened with app_name/app_category when the bot is not an Octo assistant')` |
| `octo_assistant_opened` | imperative (S3, #1451) | `openAppBotConversation` — user opens an app-bot conversation whose uid ∈ `octoAssistantUids`; props `source: 'app_bot_list'` | uid not an Octo assistant (→ `app_opened`); config unresolved (`octoAssistantUids` defaults `[]` before appconfig lands → falls through to app_opened) | Fires once per open; only the app-bot-list entry is instrumented today | appBotConversation.test.tsx `it('tracks octo_assistant_opened when the bot uid is an Octo assistant')` |
| `octo_assistant_queried` | imperative (S3, #1451) | `Conversation/index.tsx` after `sendMessage` resolves on a ChannelTypePerson channel whose `stripSpacePrefix(channelID)` ∈ `octoAssistantUids`(Space 部署下 Person channelID 形如 `s<hex>_<uid>`,不 strip 则 includes 恒 false → 助手 DM 永不发 query,P1-2); props include `intent_tag` (classifier) | Non-assistant channels; collection disabled; non-`MessageText` sends (sticker/file/forward — 已收口到 `content instanceof MessageText`) | Fires per local `MessageText` send enqueue (not gated on sendack); entry-agnostic | dapEventCoverage.pins.test.ts(botUid stripSpacePrefix) |
| `botfather_opened` | imperative | `ChatContentPage.componentDidMount` when the page mounts on the BotFather DM (`channelType===ChannelTypePerson && (cid==="botfather" || cid.endsWith("_botfather"))`) — i.e. a user enters the BotFather conversation. 门用**后缀匹配**:Space 部署下 channelID = `s{spaceId}_botfather`(spaceId 任意串,如 `sminglue_default_botfather`,`pkg/space/channel_test.go:12` 钉),裸 `botfather` 只在无 Space 时出现;对齐后端 `modules/botfather/api.go:168` `HasSuffix("_botfather")`。**不能用 `stripSpacePrefix`**:其正则 `^s[0-9a-f]{32}_` 只认 32-hex spaceId,脱不掉 `minglue_default` 这类真实 id → 硬等 `==="botfather"` 会让分母在真实 Space 部署恒 0(PR #1510 P1-1)。同一后缀门也用于 numerator(`Conversation/vm.ts` botfather 命令),两侧同步。props `{ entry }` — 来源枚举:`contact_banner`(**仅**通讯录顶端 BotFather 横幅 `dmworkcontacts/src/Contacts/index.tsx` `renderBotFatherBanner` 的 onClick,写在调用 `handleContactClick` 之前——不写在 `handleContactClick` 内,因该函数另有空间成员行/已加 AI 行/联系人行三个调用点会误标,PR #1510 P2-1)/ `conversation`(缺省:会话列表点行 `Pages/Chat/index.tsx` `onConversationClick`→`showConversation` / 深链 / 路由恢复 / 通讯录非横幅入口点到 botfather)。各入口在 `showConversation` 前写 `WKApp.shared.pendingBotfatherOpenEntry` sentinel,挂载处一次性消费并清空,未写入即缺省。**Single emit point covers every entry** because `ChatContentPage` is rendered with `key={channel.getChannelKey()}`(`channelID-channelType`, `EndpointCommon.tsx:169`) so any channel switch changes the key → remount → fresh `componentDidMount`. DAP「通讯录 · BotFather 命令使用分布」图分母 = 进入 botfather 会话的去重用户;actor_id 由 collector 附 | Non-botfather mounts (gated on channelID); never collects 正文/PII (props 仅来源枚举). **No `componentDidUpdate` emit** — with the key-based remount, `componentDidUpdate` 的 `channelChanged`(channelID/Type 变)永不为真,那条路径会是不可达死代码,故不设(PR #1510 review) | One emit per mount = 一次进入发一次;mount 与 didUpdate 不会双发(换频道即 remount → 单次 componentDidMount,且不存在 didUpdate 发点)。分母去重由 DAP/collector 按 actor_id 归并。entry 为 best-effort 来源标记:若在已处于 botfather 会话时再点入口(不 remount),sentinel 会残留到下一次进入 → 该次来源可能被误标,不影响分母 | `botfatherChannel.test.ts`(门判定 pin:钉 `sminglue_default_botfather` 等 Space-prefixed id 必命中、非 botfather 不命中;分子分母共用 `isBotfatherChannelID`)。`channelUniqueness.test.ts` 的 `IMPERATIVE_RE` 另收集 `botfather_opened` 且因其不在任何 rule 表 → 互斥断言仍通过 |
| `bot_create_started` | helper | `trackMessageSent()` on sendack Normal (`reasonCode===1`) when `intent.botCreateEntry` is set — a `/newbot` to botfather recognized by command-prefix; frontend "started" semantic only (quality=submitted), props `{entry, object_id}` | No backend `completed` counterpart is tracked (never enters the "创建成功" denominator); never collects content.text/正文 (props are enum/type/bool only); fail-closed no-op while `!isEnabled()`; no matching intent (e.g. forward) → no-op | `intents.delete(clientSeq)` on consume → idempotent, never double-counts even if old-VM path coexists; `intents` cleared via `onDisabled` hook on stop-collecting | — |
| `market_category_filtered` | imperative | `McpMarketListPage.handleCategory` / `CategoryChips.choose` — only when the selected category actually changes (`nextSel[0]!==prevSel[0] || length change`; chip guard `categoryId!==activeId`) | Repeat-click of an already-selected category / empty-state click of "all" (would inflate; the old TrackRules pill/chip DOM rule that also fired on these was removed, review P2-7) | Change-gate at call site; the DOM TrackRule was removed to avoid double-count | `dmworkbase/src/Service/__tests__/FetchRules.test.ts` it '这些「UI 采集专属」事件名不得再出现在 path 通道规则表里' — fetch-channel negative/leak guard only, NOT a dedicated imperative-fire test |
| `market_manual_publish_json_imported` | imperative | `McpCreateModal.handleApplyImport` — after the "import applied" Toast.success, when the user applies pasted JSON to the create form (seeds env/header entries, switches to manual mode, step 0) | — | — | — |
| `market_manual_publish_dialog_opened` | imperative (S3, #1451) | `McpMarketListPage` / `SkillListPage` onClick of the manual-publish method control — user opens the manual-publish dialog | — | Co-fires with `market_publish_method_selected{method:'manual'}` on the same click by design — octo-dap 手动发布漏斗(mk7 指标核查表)确认二者是**两个各自独立的必需漏斗步**:`entry_clicked → method_selected → manual_dialog_opened → json_imported → submitted`。method_selected 记录「选了 manual 这个方式」,dialog_opened 记录「manual 弹窗真的打开了」;不是重复,不合并、不删除(#1452 review P2-3) | — (new) |
| `market_mcp_connect_prompt_copied` | imperative — `McpDetailModal` `QuickAccess.handleCopy` (detail 快速接入), and the shared `PromptForwardActions.copyTrackEvent` passed as `"market_mcp_connect_prompt_copied"` by `McpConnectModal` (card 添加到 Bot); both fire only after the clipboard write resolves | On click before the promise settles; on failure/permission-denied (the catch shows Toast.error and does not track) | Post-resolve imperative on both surfaces; the card 添加到 Bot path (McpConnectModal) is now the primary connect surface | — |
| `market_module_entered` | imperative | `module.tsx` menu `onPress` when the MCP market menu is clicked — only when `!reentry` (host passes `reentry` when `prevMenuId===id`) | Repeat-click of the already-active market menu (二审 P2-4) | `!reentry` gate | — |
| `market_searched` | imperative | `McpMarketListPage.handleKeyword` (300ms debounce, after `loadData`) / `useSkills` debounce effect (after `setDebouncedQuery`) — only if `value.trim()` / `query.trim()` is non-empty | Empty/whitespace-only query (`trim()` falsy) | 300ms debounce; fire-and-forget after the debounce main logic — `track()` internally wraps in `safe()`, so a Dap error (e.g. unmocked in tests) is swallowed, never blocking the search | — |
| `market_skill_install_prompt_copied` | imperative — shared `PromptForwardActions.copyTrackEvent`, passed as `"market_skill_install_prompt_copied"` by `InstallPromptModal`; fires only after `copyToClipboard` resolves true | User copies the skill 添加到 Bot prompt | On click before the copy resolves; permission-denied / non-secure context | The modal now renders the shared `PromptForwardModal` (unified forward-to-Bot flow) instead of its own copy button, so the event moved to the shared component's `copyTrackEvent` prop (feat/market-ui-restructure) | — |
| `market_tab_switched` | imperative | `MarketSidebar.handleClick` — only when switching to a different tab (`item.id!==this.state.activeId`) | Repeat-click of the current tab (old TrackRules `market-sidebar-item` DOM rule that also fired on this was removed — inflation, review P2-7) | id-change gate; DOM rule removed to avoid double-count | `dmworkbase/src/Service/__tests__/FetchRules.test.ts` it '这些「UI 采集专属」事件名不得再出现在 path 通道规则表里' — fetch-channel negative/leak guard only, NOT a dedicated imperative-fire test |
| `market_tag_filtered` | imperative | `McpMarketListPage.handleToggleTag` / `handleClearTags`; `SkillListPage.handleSelectedTagsChange` — user toggles or clears a tag filter (select/deselect/clear-all all count as one filter action) | `handleClearTags` early-returns when `tagsSelected` is already empty (clearing an empty selection doesn't fire); originally misused `GET /mcp_tags` load inference (tag list also used for suggestions/init) | Clear-tags early-return when already empty; clear + per-toggle share the same `tagsSelected`/`loadData`, counted together (二审 P2-3) | `dmworkbase/src/Service/__tests__/FetchRules.test.ts` it '市场 mine 列表 / tag 列表加载不被当成切视图 / 选标签(也用于建议/初始化)' (+ leak-check it) — fetch-channel negative guard only, NOT a dedicated imperative-fire test |
| `market_mcp_sorted` | imperative — `McpMarketListPage.handleSortChange`, only when the sort actually changes | User changes the connector market sort (最新/最热) | Selecting the already-active sort (same-value guard returns early) | New in the market UI restructure — 最新/最热 sort added to the connector market | — |
| `market_view_switched` | RETIRED — no longer emitted | Was: user switched the 全部/我的 in-page tab strip | n/a | The all/mine tab strips were removed in the market UI restructure; 我的发布 is now the standalone `/mcp-market/mine` page (a route change, not an in-page view switch), so `handleMode` / `switchTab` and this event were deleted (feat/market-ui-restructure). Kept for historical reference | — |


## 5. `imperative` — groups/contacts, settings & misc

| Event | Channel | Fires exactly when | Must NOT fire (negative/failure edge) | Reset/dedup | Pinning test |
|---|---|---|---|---|---|
| `apps_searched` | imperative | App-bot list keyword filter: 400ms after last keystroke when `keyword.trim()` is non-empty (pure client-side filter, debounced fallback) | Empty/cleared keyword (清空不计); keyword value never captured (props恒空) | 400ms setTimeout, cleared on each keyword change | — |
| `contact_opened` | imperative | `Contacts.handleContactClick` — user taps a contact/bot row; props `object_id`=被点联系人/bot uid (显式传入,非 collectDatasetProps),`is_ai` + `bot_type` (system=botfather, else custom; null for non-bot) | Not from GET /users/:id (generic profile fetch; bots/internal lookups also hit it) | none (fires per click) | FetchRules.test.ts — `it('GET /users/:id 不产出 contact_opened(通用 profile 拉取,bot/内部查库都打)')` |
| `group_card_opened` | imperative | `Contacts.handleGroupClick` — 通讯录内点群行**弹出群名片**(唯一收口点,搜索结果行 + 列表行两处 onClick 都走这里)。与 `contact_opened`(打开人/bot 名片)对称。props `{object_id=群 group_no}`(原始值,与本文件其他 opened 事件同口径,不 stripSpacePrefix) | 仅「打开卡片」这一手势;从卡片点「进入聊天」是另一事件 `channel_opened`(`GroupCard.onEnterChat`) | fires per click(无去重) | groupCardOpenedTracking.pins.test.ts(dmworkcontacts;源码层钉发点在 handleGroupClick + object_id=group_no) |
| `contact_message_clicked` | imperative | 用户在**通讯录页**联系人名片点「发消息」发起会话。两收口点均为**通讯录页自有实例**:①真人 → `Contacts` 自有 `<UserInfo trackContactMessageEntry>` 的发消息按钮(`UserInfo/index.tsx`,按 `trackContactMessageEntry` prop 门控),`contact_type='user'`;②bot/AI → `Contacts` 自有 `<BotDetailModal onChat>`(`Contacts/index.tsx`),`contact_type='ai'`。`handleContactClick` 按 `robot===1` 分流(真人→UserInfo、bot→BotDetailModal),故 UserInfo 分支实际只出 `user`。props `{object_id=对方 uid, contact_type, space_id?}` | **B1 关键**:`UserInfo` / `BotDetailModal` 是全局共享组件(会话内名片 / 黑名单 / Bot Store / 订阅者列表 / 全局搜索 / WKBase 等均渲染);那些入口**不发**本事件——UserInfo 靠 `trackContactMessageEntry` 默认 false 门控,BotDetailModal 靠「emit 只写在通讯录页自有实例的 onChat、不在组件内部 handleChat」实现。botfather 被 `handleContactClick` 直接送进会话、不过卡片,故也不发。纯本地 `showConversation` 无 API,故不能走 fetch/body | fires per click;`contact_type` 区分真人(`user`)/AI(`ai`)——与 `contact_opened` 的 `is_ai`+`bot_type` 同概念的两种形状,映射:`user`↔`is_ai:false`、`ai`↔`is_ai:true`(本事件不带 bot_type)。`space_id`:Space 部署下为真实 id;非 Space 分支 `currentSpaceId===""`,emit 处以 `\|\| undefined` 落为 `undefined` → 被 `sanitizeProps` 丢弃(缺省即非 Space,不发空串) | — (imperative UI-only;由 channelUniqueness 命令式 scan + FetchRules 负向守卫覆盖) |
| `card_message_setting_toggled` | imperative | Bot 管理 L3「卡片消息设置」开关被服务端**确认后**发:`BotCardSettingsVM.flush()` 成功分支(批量 `PUT robot/:id/settings` 返回 2xx),逐**已确认 key** 发。props `{bot_id, key, enabled}` | **B3 关键**:**不在** `toggle()` 里发(那是持久化前);写失败(`withRetry` 耗尽 → catch → `rollbackPending`)不计;`dropped`(点击与 flush 之间变只读)已在发送前剔除,也不计;flush 在飞时切换 bot(`epoch !== myEpoch`)则在 emit 前 return,该批即使服务端已接受也不计(避免误记到新 bot)。与 `conversation_muted`/`group_bot_free_mention_toggled` 的「await 成功后发」一致 | fires per server-confirmed toggle;连点同一 key 在 flight 中被压批 → 按实际被确认的批次发(净变化一次一发)。批量 PUT body 分不出改了哪 key/新值 → 只能命令式带 `key`+`enabled` | — (imperative UI-only;channelUniqueness 命令式 scan + FetchRules 负向守卫) |
| `contacts_filter_switched` | imperative | `handleFilterChange` when selected mode ≠ current filterMode | Re-selecting the current filter (guard `filterMode === mode` returns); mode not captured (props恒空) | same-value guard | — |
| `contacts_group_expanded` | imperative | `toggleSection` on the expand edge only (`willExpand` true) | Collapsing a section (`willExpand` false); section not captured (props恒空) | `willExpand` gate | — |
| `contacts_searched` | imperative | `debouncedSearch` (300ms) when keyword is non-empty after trim | Empty/blank keyword (early return, resets search state); keyword not captured | 300ms debounce | — |
| `group_avatar_edit_opened` | imperative | `openAvatarEditor` first click past the reentry gate (opening false→true; then awaits network refresh before the editor opens) | In-flight repeat clicks while opening (opening guard returns) | `opening` reentry flag | — |
| `group_info_panel_opened` | imperative | Chat-header info click on the open edge (`!showChannelSetting`) AND channelType === Group | Closing the panel; non-group channel; props恒空 | open-edge + group gate | — |
| `group_qrcode_invite_link_copied` | imperative | `ChannelQRCode.handleCopyLink` ok branch — `copyToClipboard` resolved true (copy succeeded) | On click before the promise resolves; copy failure (else → Toast.error) | none | — |
| `http_request` | imperative | Any completed first-party (same-origin) fetch/XHR; records method, sanitized path, status_bucket, duration_ms (2xx also emits mapped body/path event) | Cross-origin/third-party requests; the telemetry batch channel itself; aborted requests (search-abort ≠ failure, not logged as status 0/'err') | none; kill-switch (enabled=false) returns early | Dap.httpAbort.test.ts — `it('fetch: AbortError is skipped; a real network failure still records err')` |
| `settings_voice_opened` | imperative | VoiceSettingsPanel mount (useEffect []) = one open of voice settings | Not from GET /voice/local-config 2xx (settings-page focus refresh also calls it) | mount-once | FetchRules.test.ts — `it('GET /voice/local-config 不产出 settings_voice_opened(设置页焦点刷新会连带调)')` |
| `template_created` | imperative | `handleTemplateSave` after `api.createCustomTopicTemplate` resolves (真创建成功); carries object_id + template_type=summary_topic | On Save click (would count server-rejected/cancelled attempts); the edit/update branches (only true create fires) | success-gated (post-await) | — |


## 6. fleet (Loop) + doc modules — natively captured via the shared octo-web bundle

`@dmwork/loop` and `@octo/docs` are compiled into the octo-web bundle and registered via
`WKApp.shared.registerModule` — in **enterprise builds** they arrive through the
`virtual:octo-enterprise-modules` slot (`apps/web/src/index.tsx`), so their axios/XHR traffic rides
the global `Dap.installHttpWrap` and their DOM rides the global click/keyboard delegation. The T1
re-review (2026-08-18) falsified the earlier "separate SPA, un-instrumentable" assumption: these
events are octo-web-native after all and belong in this convergence artifact (superseding the
header's original "Out of scope: 24 octo-docs, 133 octo-fleet" note). Wired by dap350 (PR #1443).

Scope note: only the **central-mapping channels** (fetch / body / track-testid) are enumerated here —
these are the rules that live in octo-web's FetchRules/BodyRules/TrackRules and are guarded by
`dapEventsCoverage.test.ts` (every rule-table event must appear in this table). The additional
`imperative` / `data-track` fleet/doc events emitted from inside the dmloop / docs **module source**
(e.g. `project_status_changed`, `expert_deleted`, `document_edited`, all `whiteboard_*` / `table_*`)
live in those packages' own call sites and are tracked by B-loop / B-docs, not restated here.
**R6 move**: fifteen nav / open events that central-table channels could not express correctly were
relocated to loop-module imperative emission — the seven sidebar `*_module_entered` / `*_viewed`
(`my_tasks_viewed`, `task_board_viewed`, `project_module_entered`, `automation_module_entered`,
`expert_module_entered`, `expert_team_module_entered`, `workspace_settings_opened`) plus the two
dmpersonal tab views (`runtime_tab_viewed`, `skills_tab_viewed`) behind `openTab`'s `!reentry` gate
(P1-1), and the six `*_opened` (`task_opened`, `project_opened`, `automation_opened`, `expert_opened`,
`expert_team_opened`, `skill_opened`) at each page's `openDetail` handler (P1-2). Their rows below are
kept for traceability with `Channel = imperative (loop)` and point at the dmloop / dmpersonal site.

### 6.1 fleet / doc — `fetch` channel (path → event on 2xx)

> R6:本节内六个 `*_opened` 行的 `Channel` 已改为 `imperative (loop)`(从 fetch 通道移出,见 §6 R6 move);其余行仍为 fetch。以每行 `Channel` 单元格为准。

| Event | Channel | Fires exactly when | Must NOT fire (negative/failure edge) | Reset/dedup | Pinning test |
|---|---|---|---|---|---|
| `automation_created` | fetch | POST `/fleet/api/v1/autopilots` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `automation_deleted` | fetch | DELETE `/fleet/api/v1/autopilots/:id` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `automation_opened` | imperative (loop) | dmloop `AutomationPage.openDetail(a)` → `automation_opened` | 仅用户点开手势;列表 reload / 详情轮询 / 深链预取不触发 | 从 fetch 通道移出 (R6 P1-2):同 `task_opened`(`contact_opened` 同款处理) | dmloop SkillPage/SquadPage openDetail 打点用例(同款漏斗) |
| `automation_run_manually` | fetch | POST `/fleet/api/v1/autopilots/:id/trigger` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `automation_trigger_added` | fetch | POST `/fleet/api/v1/autopilots/:id/triggers` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `automation_trigger_deleted` | fetch | DELETE `/fleet/api/v1/autopilots/:id/triggers/:id` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `document_commented` | fetch | POST `/api/v1/docs/:id/comments` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `document_created` | fetch | POST `/api/v1/docs` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `document_deleted` | fetch | DELETE `/api/v1/docs/:id` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `document_exported` | fetch | GET `/api/v1/docs/:id/export/file` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `document_forwarded` | imperative (host) | `WKBase.runDocForward` **文档卡片分享转发**(`DocForwardOpen.shareAsCard === true`,入口 `doc-forward-btn`)`ForwardService.send` 后 ≥1 目标送达 → `Dap.shared.track('document_forwarded', {})` | 全部目标失败(all-failed)不发;**html-doc「让 AI 处理」AI 指令转发不计**(`shareAsCard` 未设/false,复用同一 `runDocForward` 但属 AI 工作流手势,非 `document_forward_panel_opened` 的入口 → 计入会使转化率虚高,Octo-Q head 86c932a5 🔴);**不再挂** `POST /docs/:id/forward-grant/batch`(授权开关默认关 → 默认路径漏计,head 258e876e P1) | 每次分享转发手势一次(仅 `shareAsCard===true`;与 `message_forwarded` 同款,不受授权开关影响) | forwardResultToast.test.ts — `shouldEmitDocForwarded` predicate 真值表;**WKBase.helpers.test.tsx — emission wiring pin(在跑 harness):share(`shareAsCard:true`)送达成功=1 且 props `{}` / all-failed=0 / instruction(`shareAsCard:false`)送达成功=0**(两个 wiring mutation——call-site 换回 ungated、删 track 行——均验证可杀);FetchRules.test.ts 负向 pin(forward-grant/batch 不再映射) |
| `document_module_entered` | imperative (host nav) | host 导航真正切到文档菜单(`apps/web` Main/index.tsx + tab_low_screen.tsx,`menus.id==='docs'` 且非 reentry)→ `Dap.shared.track('document_module_entered', {})` | 重复点当前菜单(reentry)不计;**不再挂** `GET /docs/recent/creators`(该端点被 122 `?creator=` 筛选共用、任何重拉都重发 → 顶端无界放大,Octo-Q head 258e876e P1) | 每次进入手势一次(`!reentry` 门,与 `contacts_module_entered`/`apps_module_entered` 同款)—— Class N 表内无残留不变量恢复自洽 | FetchRules.test.ts `uiOnly` pin(钉死永不得回 FETCH_RULES) |
| `document_opened` | fetch | POST `/api/v1/docs/:id/view` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `document_share_managed` | fetch | PUT `/api/v1/docs/:id/members` (member role change) / DELETE `/api/v1/docs/:id/members/:seg` (member removal) → 2xx — the two **write** endpoints of the share-member panel feed one event (R13 B4) | Non-2xx / cross-origin; GET `/api/v1/docs/:id/members` (panel-open member-list load) NOT counted — a read manages nothing and re-GETs after a real write would double-count; panel-open signal, if ever wanted, belongs in a dedicated `*_panel_opened` event | None — per successful request | FetchRules.test.ts — it('成员管理 PUT/DELETE 归一 document_share_managed;GET(打开面板)与 creators 不计') pins GET → undefined + PUT/DELETE → event |
| `expert_created` | fetch | POST `/fleet/api/v1/agents` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `expert_opened` | imperative (loop) | dmloop `AgentPage.openDetail(id)` → `expert_opened` | 仅用户点开手势;列表 reload / 详情轮询 / 深链预取不触发 | 从 fetch 通道移出 (R6 P1-2):同 `task_opened`(`contact_opened` 同款处理) | dmloop SkillPage/SquadPage openDetail 打点用例(同款漏斗) |
| `expert_team_created` | fetch | POST `/fleet/api/v1/squads` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `expert_team_member_removed` | fetch | DELETE `/fleet/api/v1/squads/:id/members` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `expert_team_opened` | imperative (loop) | dmloop `SquadPage.openDetail(id)` → `expert_team_opened` | 仅用户点开手势;列表 reload / 详情轮询 / 深链预取不触发;行内动作按钮 `stopPropagation` 不误开 | 从 fetch 通道移出 (R6 P1-2):同 `task_opened`(`contact_opened` 同款处理) | dmloop SquadPage.test.tsx — 行点开发 `expert_team_opened`、动作按钮不触发 |
| `expert_unarchived` | fetch | POST `/fleet/api/v1/agents/:id/restore` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `project_created` | fetch | POST `/fleet/api/v1/projects` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `project_deleted` | fetch | DELETE `/fleet/api/v1/projects/:id` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `project_opened` | imperative (loop) | dmloop `ProjectPage.openDetail(id)` → `project_opened` | 仅用户点开手势;列表 reload / 详情轮询 / 深链预取不触发 | 从 fetch 通道移出 (R6 P1-2):同 `task_opened`(`contact_opened` 同款处理) | dmloop SkillPage/SquadPage openDetail 打点用例(同款漏斗) |
| `runtime_machine_renamed` | body | PATCH `/fleet/api/v1/runtimes/:id` has `name` on 2xx | discriminator key absent(非改名的 runtime PATCH)/ wrong endpoint / non-JSON body | presence discriminator, no fallback(仅带 `name` 键才计,与 `automation_renamed` 同款;Octo-Q head 258e876e P2:从 fetch 泛 PATCH 迁入,避免任何 runtime PATCH 都误计为改名) | BodyRules.test.ts — fleet body 键通道真实命中(`{name}`→命中,`{cpu}`→不命中) |
| `skill_created` | fetch | POST `/fleet/api/v1/skills` / POST `/fleet/api/v1/skills/import` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `skill_deleted` | fetch | DELETE `/fleet/api/v1/skills/:id` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `skill_opened` | imperative (loop) | dmloop `SkillPage.openDetail(id)` → `skill_opened` | 仅用户点开手势;列表 reload / 详情轮询 / 深链预取不触发 | 从 fetch 通道移出 (R6 P1-2):同 `task_opened`(`contact_opened` 同款处理) | dmloop SkillPage.test.tsx — 行点开发 `skill_opened` 恰一次 |
| `skill_runtime_skills_pulled` | fetch | POST `/fleet/api/v1/runtimes/:id/local-skills` → 2xx | Non-2xx / cross-origin | **请求已受理口径(非完成率)** — 该端点仅「受理」拉取请求,真完成要 GET `/local-skills/:id` 轮询到 `completed`(Octo-Q head 258e876e P2);本行按「拉取已发起/受理」采集,不读作完成数;900ms 轮询 GET 不计 | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `skill_saved` | fetch | PUT `/fleet/api/v1/skills/:id` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| ~~`task_board_filtered`~~ | ~~fetch~~ | 从 path 通道移除 (R13 B2) — GET `/issues` / `/grouped` / `/search` 均为任务板列表 reload 端点,一次手势即触发,fetch 通道表达不了「筛选」意图且会与 `task_board_viewed` 双发。精确到筛选/搜索手势的 `task_board_filtered` 改由 loop 模块命令式发射 (B-loop),不在本中央表列举。**B-loop 已落地** (2026-09-10): octo-loop-module MR !65 commit `31730f7` — IssuePage `update()` 漏斗命令式发射,关键词 400ms 防抖 / 空串与 clearFilters(重置)与视图·scope 切换静默。R6 续:`task_board_viewed`/`task_opened` 亦一并移出(见各自行),`/issues` 全系不再挂任何 path 规则,原压 `/issues/:id` 的 `FETCH_IGNORE` 随 `task_opened` 一并删除 | — | — | FetchRules.test.ts — 断言 `/issues`·`/grouped`·`/search`·`/issues/:id` 四者均 `undefined` |
| `task_commented` | fetch | POST `/fleet/api/v1/issues/:id/comments` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `task_deleted` | fetch | DELETE `/fleet/api/v1/issues/:id` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `task_opened` | imperative (loop) | dmloop `IssuePage.openDetail(id)` → `task_opened` | 仅用户点开手势;列表 reload / 详情轮询 / 深链预取不触发 | 从 fetch 通道移出 (R6 P1-2):GET `/issues/:id` 分不清点开与预取/轮询,`Dap.track` 无去重会双计;`contact_opened` 同款处理 | dmloop SkillPage/SquadPage openDetail 打点用例(同款漏斗) |
| `workspace_created` | fetch | POST `/fleet/api/v1/workspaces` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `workspace_general_saved` | fetch | PATCH `/fleet/api/v1/workspaces/:id` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `workspace_member_added` | fetch | POST `/fleet/api/v1/workspaces/:id/octo-members` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `workspace_member_removed` | fetch | DELETE `/fleet/api/v1/workspaces/:id/members/:id` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |
| `workspace_member_role_changed` | fetch | PATCH `/fleet/api/v1/workspaces/:id/members/:id` → 2xx | Non-2xx / cross-origin | None — per successful request | — (generic FetchRules path-hit invariant + fleet/doc path cases in FetchRules.test.ts) |

### 6.2 fleet — `body` channel (request-body top-level key → event on 2xx)

| Event | Channel | Fires exactly when | Must NOT fire (negative/failure edge) | Reset/dedup | Pinning test |
|---|---|---|---|---|---|
| `automation_enabled_toggled` | body | PATCH `/fleet/api/v1/autopilots/:id` `status`∈{active,paused} on 2xx | discriminator key absent / wrong endpoint / non-JSON body | presence-or-enum discriminator; per 2xx | BodyRules.test.ts — 'fleet body 键通道真实命中' |
| `automation_executor_changed` | body | PATCH `/fleet/api/v1/autopilots/:id` has `assignee_type`+`assignee_id` on 2xx | discriminator key absent / wrong endpoint / non-JSON body | presence-or-enum discriminator; per 2xx | BodyRules.test.ts — 'fleet body 键通道真实命中' |
| `automation_instruction_saved` | body | PATCH `/fleet/api/v1/autopilots/:id` has `description` on 2xx | discriminator key absent / wrong endpoint / non-JSON body | presence-or-enum discriminator; per 2xx | BodyRules.test.ts — 'fleet body 键通道真实命中' |
| `automation_renamed` | body | PATCH `/fleet/api/v1/autopilots/:id` has `title` on 2xx | discriminator key absent / wrong endpoint / non-JSON body | presence-or-enum discriminator; per 2xx | BodyRules.test.ts — 'fleet body 键通道真实命中' |
| `automation_target_project_changed` | body | PATCH `/fleet/api/v1/autopilots/:id` has `project_id` on 2xx | discriminator key absent / wrong endpoint / non-JSON body | presence-or-enum discriminator; per 2xx | BodyRules.test.ts — 'fleet body 键通道真实命中' |
| `automation_trigger_edited` | body | PATCH `/fleet/api/v1/autopilots/:id/triggers/:id` has `cron_expression` on 2xx | discriminator key absent / wrong endpoint / non-JSON body | presence-or-enum discriminator; per 2xx | BodyRules.test.ts — 'fleet body 键通道真实命中' |
| `automation_trigger_toggled` | body | PATCH `/fleet/api/v1/autopilots/:id/triggers/:id` has `enabled` on 2xx | discriminator key absent / wrong endpoint / non-JSON body | presence-or-enum discriminator; per 2xx | BodyRules.test.ts — 'fleet body 键通道真实命中' |
| `expert_team_member_added` | imperative (loop) — **pending handoff** | 计划:dmloop squad-detail 加成员成功回调 → `Dap.shared.track('expert_team_member_added', {...})`（带 squad/batch 标识，与 remove 同「每手势」单位） | **暂 hold,不进 body 通道**（Octo-Q head 258e876e P2）：body 通道无关联信号（建团批量加 N 人发 N 个同形 POST、props 恒 `{}`，中心侧无法与单独加成员区分），且与 collection-level DELETE 的 `expert_team_member_removed`（每手势 1 个）单位不一致 | 每手势一次（对齐 remove 单位）—— **已知交接依赖**：octo-loop-module dmloop 命令式站点上线前 add 不发 | dmloop squad-detail add-member 打点用例（模块侧交付） |
| `project_webhook_added` | body | POST `/fleet/api/v1/webhook-subscriptions` has `project_id` on 2xx | discriminator key absent / wrong endpoint / non-JSON body | presence-or-enum discriminator; per 2xx | BodyRules.test.ts — 'fleet body 键通道真实命中' |
| `task_assignee_changed` | body | PUT `/fleet/api/v1/issues/:id` has `assignee_id`+`assignee_type` on 2xx | discriminator key absent / wrong endpoint / non-JSON body | presence discriminator; per 2xx（判别子序见 `task_status_changed` 行：`status`>`priority`>`assignee`>`project_id`>detail，多键归更前者） | BodyRules.test.ts — 'fleet body 键通道真实命中' |
| `task_created` | body | POST `/fleet/api/v1/issues` (no discriminator key → fallback) on 2xx | discriminator key absent / wrong endpoint / non-JSON body | presence-or-enum discriminator; per 2xx | BodyRules.test.ts — 'fleet body 键通道真实命中' |
| `task_detail_edited` | body | PUT `/fleet/api/v1/issues/:id` (no discriminator key → fallback) on 2xx | discriminator key absent / wrong endpoint / non-JSON body | fallback（仅当 `status`/`priority`/`assignee`/`project_id` 全不中时；判别子序见 `task_status_changed` 行） | BodyRules.test.ts — 'fleet body 键通道真实命中' |
| `task_priority_changed` | body | PUT `/fleet/api/v1/issues/:id` has `priority` on 2xx | discriminator key absent / wrong endpoint / non-JSON body | presence discriminator; per 2xx（判别子序见 `task_status_changed` 行；`status` 与 `priority` 并存时归 `task_status_changed`） | BodyRules.test.ts — 'fleet body 键通道真实命中' |
| `task_project_changed` | body | PUT `/fleet/api/v1/issues/:id` has `project_id` on 2xx | discriminator key absent / wrong endpoint / non-JSON body | presence discriminator; per 2xx（判别子序见 `task_status_changed` 行；`status`/`priority`/`assignee` 任一并存时归更前者，如跨项目拖卡 `{status,project_id}`→`task_status_changed`） | BodyRules.test.ts — 'fleet body 键通道真实命中' |
| `task_status_changed` | body | PUT `/fleet/api/v1/issues/:id` has `status` on 2xx | discriminator key absent / wrong endpoint / non-JSON body | presence discriminator; per 2xx — **判别子首中即返序:`status` > `priority` > `assignee` > `project_id` > (fallback)`detail`**(显式决策,Octo-Q head 258e876e P2:多键 payload 如 `{status,project_id}` / 整对象 PUT 归 `task_status_changed`;由 BodyRules.test.ts 多键 + 整对象用例钉死,改序即红) | BodyRules.test.ts — 'fleet body 键通道真实命中' + 'PUT issues 多键判别子优先级' |
| `task_subtask_created` | body | POST `/fleet/api/v1/issues` has `parent_issue_id` (non-null) on 2xx | discriminator key absent / **explicit `parent_issue_id: null`/`undefined`** / wrong endpoint / non-JSON body — null/undefined is treated as key-absent (P2#1 hardening) so a top-level task with `{parent_issue_id:null}` falls to `task_created` | presence-or-enum discriminator; per 2xx | BodyRules.test.ts — 'fleet body 键通道真实命中' + 'hasKeys 空值加固' |
| `workspace_webhook_added` | body | POST `/fleet/api/v1/webhook-subscriptions` (no discriminator key → fallback) on 2xx | discriminator key absent / wrong endpoint / non-JSON body | presence-or-enum discriminator; per 2xx | BodyRules.test.ts — 'fleet body 键通道真实命中' |

### 6.3 fleet / doc — `track(testid)` channel (DOM delegation on `data-testid`)

> R6:本节内九个侧栏导航行(`my_tasks_viewed`/`task_board_viewed`/`*_module_entered`/`workspace_settings_opened`/`runtime_tab_viewed`/`skills_tab_viewed`)的 `Channel` 已改为 `imperative (loop)`(从 track 通道移出,需 `!reentry` 去重,见 §6 R6 move);其余行仍为 track。以每行 `Channel` 单元格为准。
>
> **Class N vs Class A 计数策略(R6 owner 裁定)** — DOM 点击委托通道无 reentry 门,「重复点当前项」是否重发按控件类别裁定:
> - **Class N(导航 / 入口)**:主侧栏 tab `*_module_entered` / `*_viewed`。reentry 必须 no-op —— 已在 R6 移到 loop / personal 模块侧 `openTab` 的 `if (key !== tab)` 命令式发射(见上九行 `imperative (loop)`),表内无残留。
> - **Class A(页内激活)**:scope 段 / 视图模式 / 筛选 / 排序 / 页内内容 tab / 详情子 tab 视图 / 页内 toggle。**明确按激活手势计数**,重复点当前项再计一次为**预期**(有界,中心侧按时间 / 会话聚合去重),故留在本表、走 DOM 委托、不加 reentry 门。以下九行判为 Class A:`task_board_segment_switched`、`task_board_view_switched`、`project_view_switched`、`expert_team_tab_switched`、`expert_team_filtered`、`expert_tab_switched`、`document_tab_switched`、`document_outline_toggled`、`expert_run_history_viewed`。不走跨仓 relocate(避免 octo-web CI 验不了 + 重造「文档已列未接线」缺口)。

| Event | Channel | Fires exactly when | Must NOT fire (negative/failure edge) | Reset/dedup | Pinning test |
|---|---|---|---|---|---|
| `automation_create_dialog_opened` | track(testid) — `automation-create-btn`, `automation-create-btn-empty` @`/loop` | User activates the control → `automation_create_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `automation_delete_dialog_opened` | track(testid) — `automation-delete-btn` @`/loop` | User activates the control → `automation_delete_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `automation_module_entered` | imperative (loop) | dmloop `useLoopWorkspace.openTab('automation')` → `automation_module_entered` | 重复点当前 tab = no-op(`!reentry` 门);列表 reload / 深链解析不触发 | 从 track 通道移出 (R6 P1-1):同 `openTab` `!reentry` 门 | dmloop useLoopWorkspace.test.tsx — openTab `!reentry` 门 |
| `automation_trigger_add_dialog_opened` | track(testid) — `automation-trigger-add-btn` @`/loop` | User activates the control → `automation_trigger_add_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `automation_trigger_delete_dialog_opened` | track(testid) — `automation-trigger-delete-btn` @`/loop` | User activates the control → `automation_trigger_delete_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `automation_trigger_edit_dialog_opened` | track(testid) — `automation-trigger-edit-btn` @`/loop` | User activates the control → `automation_trigger_edit_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `document_comment_input_opened` | track(testid) — `comment-bubble-start` @`/d`, `/ppt/d` | User activates the control → `document_comment_input_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText; route gate = the two standalone-doc namespaces (`/d/:docId` standard docs + `/ppt/d/:docId` slides, per documentScene.ts — NOT the same-shell doc list page; R13 B5) | `on:'click'`; multi-testid = mutually-exclusive entries | dapEventsCoverage.test.ts — route-reachability + row-token invariant |
| `document_forward_panel_opened` | track(testid) — `doc-forward-btn` | User activates the control → `document_forward_panel_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `document_history_viewed` | track(testid) — `doc-more-item-history` | User activates the control → `document_history_viewed` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `document_insert_used` | track(testid) — `doc-insert-image`, `doc-insert-file`, `doc-insert-table`, `doc-insert-bookmark`, `doc-insert-emoji`, `doc-insert-mention`, `doc-insert-details`, `doc-insert-callout`, `doc-insert-formula-inline`, `doc-insert-formula-block`, `doc-insert-link` | User activates the control → `document_insert_used` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `document_open_in_new_page` | track(testid) — `doc-more-item-open-new-page` | User activates the control → `document_open_in_new_page` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `document_outline_toggled` | track(testid) — `doc-outline-toggle` | User activates the control → `document_outline_toggled` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries; **Class A (page-internal activation, R6 owner ruling)** — open/close share one testid; each toggle activation counts once by design (bounded; center-side time/session aggregation dedups) | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `document_tab_switched` | track(testid) — `docs-tab-recent`, `docs-tab-mine` | User activates the control → `document_tab_switched` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries; **Class A (page-internal activation, R6 owner ruling)** — counted per activation gesture, so re-clicking the current tab re-fires by design (bounded; center-side time/session aggregation dedups) | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `expert_archive_dialog_opened` | track(testid) — `loop-agent-archive-btn` | User activates the control → `expert_archive_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `expert_create_dialog_opened` | track(testid) — `loop-agent-create-btn` | User activates the control → `expert_create_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `expert_env_var_add_dialog_opened` | track(testid) — `loop-agent-add-env-btn` | User activates the control → `expert_env_var_add_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `expert_module_entered` | imperative (loop) | dmloop `useLoopWorkspace.openTab('agent')` → `expert_module_entered` | 重复点当前 tab = no-op(`!reentry` 门);列表 reload / 深链解析不触发 | 从 track 通道移出 (R6 P1-1):同 `openTab` `!reentry` 门 | dmloop useLoopWorkspace.test.tsx — openTab `!reentry` 门 |
| `expert_run_history_viewed` | track(testid) — `loop-agent-tab-profile` | User activates the control → `expert_run_history_viewed` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries; **Class A (page-internal activation, R6 owner ruling)** — counted per activation gesture, so re-clicking the current detail sub-tab re-fires by design (bounded; center-side time/session aggregation dedups) | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `expert_searched` | track(testid) — `loop-agent-search-input` | User focus-clicks the search input → counts **box activation**, not the query: every caret-placement click fires once; typed query strings are NOT counted (per-query debounced imperative emission, the `smart_summary_searched` treatment, is deferred to the loop module as a B-loop fast-follow) | Ancestor `data-track` wins; keydown/submit skipped (never fires per keystroke); never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | TrackRules.rules.test.ts — it("*_searched keeps the on:'click' activation gate (R13 B3 pin)") — stripping `on:'click'` goes red |
| `expert_skill_add_dialog_opened` | track(testid) — `loop-agent-add-skill-btn` | User activates the control → `expert_skill_add_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `expert_tab_switched` | track(testid) — `loop-agent-scope-mine`, `loop-agent-scope-all`, `loop-agent-scope-archived` | User activates the control → `expert_tab_switched` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries; **Class A (page-internal activation, R6 owner ruling)** — counted per activation gesture, so re-clicking the current item re-fires by design (bounded; center-side time/session aggregation dedups) | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `expert_team_archive_dialog_opened` | track(testid) — `loop-squad-archive-btn` | User activates the control → `expert_team_archive_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `expert_team_create_dialog_opened` | track(testid) — `loop-squad-create-btn` | User activates the control → `expert_team_create_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `expert_team_delete_dialog_opened` | track(testid) — `loop-squad-row-delete-btn` | User activates the control → `expert_team_delete_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `expert_team_filtered` | track(testid) — `loop-squad-filter-leader`, `loop-squad-filter-creator`, `loop-squad-filter-clear`, `loop-squad-sort-name`, `loop-squad-sort-members`, `loop-squad-sort-created`, `loop-squad-sort-dir` | User activates the control → `expert_team_filtered` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries; **Class A (page-internal activation, R6 owner ruling)** — counted per activation gesture, so re-clicking the current filter/sort re-fires by design (bounded; center-side time/session aggregation dedups) | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `expert_team_instruction_edit_opened` | track(testid) — `loop-squad-instruction-editor` | User activates the control → `expert_team_instruction_edit_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `expert_team_member_add_dialog_opened` | track(testid) — `loop-squad-add-member-btn` | User activates the control → `expert_team_member_add_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `expert_team_module_entered` | imperative (loop) | dmloop `useLoopWorkspace.openTab('squad')` → `expert_team_module_entered` | 重复点当前 tab = no-op(`!reentry` 门);列表 reload / 深链解析不触发 | 从 track 通道移出 (R6 P1-1):同 `openTab` `!reentry` 门 | dmloop useLoopWorkspace.test.tsx — openTab `!reentry` 门 |
| `expert_team_tab_switched` | track(testid) — `loop-squad-scope-mine`, `loop-squad-scope-all` | User activates the control → `expert_team_tab_switched` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries; **Class A (page-internal activation, R6 owner ruling)** — counted per activation gesture, so re-clicking the current item re-fires by design (bounded; center-side time/session aggregation dedups) | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `my_tasks_viewed` | imperative (loop) | dmloop `useLoopWorkspace.openTab('myloop')` → `my_tasks_viewed` | 重复点当前 tab = no-op(`!reentry` 门);列表 reload / 深链解析不触发 | 从 track 通道移出 (R6 P1-1):DOM 委托无法表达 `!reentry` 去重、`Dap.track` 无去重会重发;`openTab` 的 `if (key !== tab)` 门七事件一次映射 | dmloop useLoopWorkspace.test.tsx — openTab `!reentry` 门 |
| `project_create_dialog_opened` | track(testid) — `project-create-btn`, `project-create-btn-empty` @`/loop` | User activates the control → `project_create_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `project_delete_dialog_opened` | track(testid) — `project-row-delete-btn` @`/loop` | User activates the control → `project_delete_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `project_module_entered` | imperative (loop) | dmloop `useLoopWorkspace.openTab('project')` → `project_module_entered` | 重复点当前 tab = no-op(`!reentry` 门);列表 reload / 深链解析不触发 | 从 track 通道移出 (R6 P1-1):同 `openTab` `!reentry` 门 | dmloop useLoopWorkspace.test.tsx — openTab `!reentry` 门 |
| `project_searched` | track(testid) — `project-search-input` @`/loop` | User focus-clicks the search input → counts **box activation**, not the query: every caret-placement click fires once; typed query strings are NOT counted (per-query debounced imperative emission, the `smart_summary_searched` treatment, is deferred to the loop module as a B-loop fast-follow) | Ancestor `data-track` wins; keydown/submit skipped (never fires per keystroke); never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | TrackRules.rules.test.ts — it("*_searched keeps the on:'click' activation gate (R13 B3 pin)") — stripping `on:'click'` goes red |
| `project_view_switched` | track(testid) — `project-view-list`, `project-view-card` @`/loop` | User activates the control → `project_view_switched` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries; **Class A (page-internal activation, R6 owner ruling)** — counted per activation gesture, so re-clicking the current item re-fires by design (bounded; center-side time/session aggregation dedups) | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `runtime_add_computer_dialog_opened` | track(testid) — `runtime-add-computer-btn` @`/personal` | User activates the control → `runtime_add_computer_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `runtime_machine_rename_dialog_opened` | track(testid) — `runtime-rename-btn` @`/personal` | User activates the control → `runtime_machine_rename_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `runtime_tab_viewed` | imperative (loop) | dmpersonal `usePersonalWorkspace.openTab('runtime')` → `runtime_tab_viewed` | 重复点当前 tab = no-op(`!reentry` 门);机器级模式下 skill 分支提前 return | 从 track 通道移出 (R6 P1-1):同 loop `openTab` `!reentry` 门 | — (mirror of dmloop openTab 门;dmpersonal 暂无测试基建) |
| `skill_create_dialog_opened` | track(testid) — `skill-create-btn` @`/personal` | User activates the control → `skill_create_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `skill_delete_dialog_opened` | track(testid) — `skill-delete-btn` @`/personal` | User activates the control → `skill_delete_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `skill_file_opened` | track(testid) — `skill-file-tree-item` @`/personal` | User activates the control → `skill_file_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `skills_tab_viewed` | imperative (loop) | dmpersonal `usePersonalWorkspace.openTab('skill')` → `skills_tab_viewed` | 重复点当前 tab = no-op(`!reentry` 门);机器级模式下 skill 分支提前 return | 从 track 通道移出 (R6 P1-1):同 loop `openTab` `!reentry` 门 | — (mirror of dmloop openTab 门;dmpersonal 暂无测试基建) |
| `task_board_segment_switched` | track(testid) — `loop-issue-scope-all`, `loop-issue-scope-members`, `loop-issue-scope-agents` | User activates the control → `task_board_segment_switched` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries; **Class A (page-internal activation, R6 owner ruling)** — counted per activation gesture, so re-clicking the current item re-fires by design (bounded; center-side time/session aggregation dedups) | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `task_board_view_switched` | track(testid) — `loop-issue-view-board`, `loop-issue-view-grouped`, `loop-issue-view-list` | User activates the control → `task_board_view_switched` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries; **Class A (page-internal activation, R6 owner ruling)** — counted per activation gesture, so re-clicking the current item re-fires by design (bounded; center-side time/session aggregation dedups) | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `task_board_viewed` | imperative (loop) | dmloop `useLoopWorkspace.openTab('issue')` → `task_board_viewed` | 重复点当前 tab = no-op(`!reentry` 门);列表 reload / 深链解析不触发 | 从 track 通道移出 (R6 P1-1):同 `openTab` `!reentry` 门 | dmloop useLoopWorkspace.test.tsx — openTab `!reentry` 门 |
| `task_create_dialog_opened` | track(testid) — `loop-sidebar-new-issue`, `loop-issue-board-new-issue`, `loop-issue-board-empty-new-issue` | User activates the control → `task_create_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `task_delete_dialog_opened` | track(testid) — `loop-idp-menu-delete` | User activates the control → `task_delete_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `task_edit_properties_opened` | track(testid) — `loop-idp-menu-edit-props` | User activates the control → `task_edit_properties_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `task_subtask_create_dialog_opened` | track(testid) — `loop-idp-menu-new-subtask`, `loop-idp-subissue-add` | User activates the control → `task_subtask_create_dialog_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `whiteboard_find_on_canvas` | track(testid) — `toolbar-search` @`/d`, `/ppt/d` | User activates the control → `whiteboard_find_on_canvas` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText; route gate = the two standalone-doc namespaces (`/d/:docId` standard docs + `/ppt/d/:docId` slides — slides editor lives in octo-docs-module, unprovable unreachable in-repo, so the gate is widened; NOT the same-shell doc list page; R13 B5); ⚠️ Cmd+F keyboard path leaks (known gap, 待裁) | `on:'click'`; multi-testid = mutually-exclusive entries | dapEventsCoverage.test.ts — route-reachability + row-token invariant |
| `workspace_create_clicked` | track(testid) — `loop-sidebar-ws-create`, `loop-sidebar-ws-create-empty` | User activates the control → `workspace_create_clicked` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |
| `workspace_settings_opened` | imperative (loop) | dmloop `useLoopWorkspace.openTab('settings')` → `workspace_settings_opened` | 重复点当前 tab = no-op(`!reentry` 门);列表 reload / 深链解析不触发 | 从 track 通道移出 (R6 P1-1):同 `openTab` `!reentry` 门 | dmloop useLoopWorkspace.test.tsx — openTab `!reentry` 门 |
| `workspace_switcher_opened` | track(testid) — `loop-sidebar-ws-switcher` | User activates the control → `workspace_switcher_opened` | Ancestor `data-track` wins; keydown/submit skipped; never reads value/innerText | `on:'click'`; multi-testid = mutually-exclusive entries | — (no dedicated positive test; channelUniqueness guards cross-channel uniqueness) |


---

_Generated from the round-6 source of truth (§1–§5 rule tables + data-track attrs + imperative call sites) + dap350 fleet/doc rule tables (§6). 264 event rows (`grep -c '^| \`'`, includes 4 infra rows)._
